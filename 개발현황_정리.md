# UTAU Auto OTO v3 개발 현황 정리

> 주의: 2026-03-08 기준으로 OTO 자동 생성용 `PyTorch` 보정 파이프라인은 제거되었다. 아래 PyTorch 관련 메모는 과거 실험 기록 보존용이다.


## 문서 작성 규칙
- 본 문서는 앞으로 **UTF-8 인코딩 + 한국어** 기준으로만 작성/수정한다.
- 깨진 문자열(인코딩 손상 텍스트)은 발견 즉시 한글로 복구한다.

## 1) 문서 목적
- 나중에 개발을 재개할 때, 현재 구현 상태와 미해결 이슈를 빠르게 파악하기 위한 기준 문서.
- 코드 기준으로 정리했으며, 체크리스트(`task.md`)의 계획 항목과 실제 구현 상태를 함께 반영.

## 2) 기준 시점
- 정리 시점: 2026-02-27 00:15 (로컬 로그 기준)
- 기준 파일:
  - `main.py`
  - `core/*.py`
  - `logs/log_20260227_001335.txt`
  - `task.md`, `implementation_plan.md`

## 2-1) 최근 업데이트 (2026-02-27)
- `core/oto_validator.py` 검증 기준을 일본어 다중 음절 파일에 맞게 보정.
- 변경 내용:
  - `Offset seems late` 경고를 모든 alias가 아니라 **첫 alias 중심**으로 제한.
  - 일본어 모드에서 `pre/cutoff/boundary` 허용 마진을 완화해 과도한 오탐 감소.
  - `Offset before active region` 경고를 별도로 분리.
- 동일 데이터 재검증 결과:
  - `oto.ini`: warning `569 -> 53` (error 0 유지)
  - `oto.tuned.test.ini`: warning `551 -> 52` (error 0 유지)

## 2-2) 최근 업데이트 2차 튜닝 (2026-02-27)
- 대상 파일:
  - `core/ja_oto_generator.py`
  - `core/oto_validator.py`
- 생성기 개선:
  - phones tier에서 `sil/sp/pau/spn`을 음소 계산에서 제외.
  - phones tier가 비거나 희소할 때 words/파일명 기반 pseudo-phone 합성 fallback 추가.
  - trailing micro-segment(긴 공백 뒤 짧은 끝 음소) 감지 후 `effective_end_ms`로 `pre/cutoff` 과확장 억제.
  - `offset/pre/consonant/cutoff` 후처리 상한/하한 재조정(초반 과도한 early, 후반 과도한 long tail 완화).
  - 일본어 alias 분류(`classify_ja_alias`)에서 특수 연결기호/비모음 우변을 VC로 분류하도록 보완.
- 검증기 개선:
  - 일본어에서 phones 경계 수가 너무 적은 파일은 boundary-distance 경고를 생략(희소 tier 오탐 억제).
  - offset/pre 경고를 alias 타입별로 마진 차등 적용.
  - `Offset seems late`는 첫 alias이면서 비-VC/VV일 때만 경고.
  - `sequence_shift`는 일본어에서 anchor(비-VC/VV) 시퀀스를 우선 사용.
- 재검증 결과(동일 데이터):
  - `oto.tuned.test.ini`: warning `52 -> 34 -> 16 -> 2` (error 0 유지)
  - 현재 잔여 경고 2건:
    - `_つぃ_つ_つぇ_つぉ.wav` (`- つぃ` offset early)
    - `_じ_じゅ_じぇ_じょ.wav` (`- じ` offset early)

## 2-3) 내부 자동 튜닝 프로파일(워크플로우 유지) (2026-02-27)
- 사용자 워크플로우는 기존과 동일(버튼/입력 항목 변경 없음).
- 일본어 OTO 생성 시 내부 동작:
  - 출력 폴더에 `.ja_oto_autotune_profile.json`이 있으면 자동 로드하여 생성 파라미터 보정 적용.
  - 출력 폴더에 수동 정답 OTO가 있으면 자동 학습 후 프로파일 갱신.
- 정답 OTO 자동 탐색 파일명(출력 폴더):
  - `oto.manual.ini`
  - `oto.correct.ini`
  - `oto.reference.ini`
  - `oto.gold.ini`
  - `oto.human.ini`
- 환경변수로 정답 OTO 직접 지정 가능:
  - `UTOA_JA_REF_OTO=<정답_oto_절대경로>`
- 학습 직후 첫 1회는 같은 실행에서 부트스트랩 적용(생성 결과 파일 재보정)됨.
- 관련 코드:
  - `core/ja_oto_generator.py`
    - `_train_ja_autotune_profile`
    - `_apply_ja_autotune_profile`
    - `_apply_profile_to_oto_file`

## 2-4) 한국어 기준 프로파일(Yeonu/SoYoon 기반) (2026-02-27)
- 기준 데이터를 직접 포함하지 않도록, 통계값만 추상화해 별도 코드로 분리:
  - `core/oto_profile_presets.py` (`KR_PROFILE_PRESET_V1`)
- 로컬 참조셋(Yeonu/SoYoon)으로 산출한 통계만 반영하고 원본 파일/경로는 저장하지 않음.
- 내부 동작:
  - 한국어 OTO 생성 시 `assets/profiles/kr_oto_reference_profile.json`을 자동 로드.
  - 프로파일 파일이 없으면 `core/oto_profile_presets.py`의 추상 프리셋을 캐시로 생성.
  - 필요 시에만 환경변수 기반 로컬 재학습(`UTOA_KR_PROFILE_DIRS`) 수행.
- 생성 후 결과 OTO에 타입별(`cv/cv_head/vc/vcv/vv`) 통계 보정을 약하게 적용.
- 안전장치:
  - 보정 강도는 샘플 수 기반으로 제한(가중치 상한).
  - `pre/cons/cutoff/ovl`는 클램프 후 `validate_oto_params` 재검증.
- 환경변수로 기준 폴더 변경 가능:
  - `UTOA_KR_PROFILE_DIRS=dir1;dir2;...`
- 환경변수로 외부 프로파일 파일 직접 지정 가능:
  - `UTOA_KR_PROFILE_JSON=<profile.json 경로>`
- 현재 생성된 프로파일 요약:
  - rows: `2224`, wavs: `223`, buckets: `5`
  - 저장 경로: `assets/profiles/kr_oto_reference_profile.json`

## 2-5) 일본어 추상 프리셋(VCV/CVVC) 추가 (2026-02-27)
- 원본 보이스뱅크의 이름/경로를 코드에 남기지 않도록, 통계값만 추상화 프리셋으로 분리.
- 추가 위치:
  - `core/oto_profile_presets.py`
  - `JA_PROFILE_PRESET_VCV_V1`
  - `JA_PROFILE_PRESET_CVVC_V1`
  - `get_ja_profile_preset(format_type)`
- 생성기 반영:
  - `core/ja_oto_generator.py`에서 포맷(`vcv/cvvc`)별 프리셋 선택 가능.
  - 기본값은 안전을 위해 프리셋 직접 적용 OFF (기존 품질 유지).
  - 활성화 시 환경변수 사용:
    - `UTOA_JA_USE_STYLE_PRESET=1`
- 경로/이름 비식별화:
  - 일본어 자동튜닝 프로파일 저장 시 절대경로(`source_auto/source_ref`)는 제거.
  - 파일명 수준 메타(`source_auto_name/source_ref_name`)만 유지.

## 2-6) 로그 제외 항목 집계 + 수동 일부 OTO 기반 미세조정 탭 추가 (2026-02-27)
- 대상 파일:
  - `core/oto_generator.py`
  - `core/ja_oto_generator.py`
  - `main.py`
- 공통 로그 강화:
  - 한국어/일본어 생성기 모두에서 자동 설정 제외 항목을 사유별로 집계해 **로그 마지막에 요약 출력**.
  - 집계 사유 예:
    - `empty_alias`
    - `malformed_line`
    - `textgrid_missing`
    - `tier_missing`
    - `empty_intervals`
    - `mapping_failed`
    - `file_exception`
  - 각 사유별 건수 + 예시 파일/alias 1건 출력.
- 수동 일부 OTO 기반 프로파일 미세조정:
  - GUI에 새 탭 추가: `🧩 프로파일 미세조정`
  - 입력:
    - 자동 OTO(기준)
    - 수동 OTO(부분 보정본)
    - 프로파일 저장 경로(JSON)
    - 적용 대상 OTO
  - 동작:
    - 두 OTO를 `wav + alias(+등장순서)` 기준으로 매칭
    - 타입별(`cv`, `vc`, `vcv`, `vv`, `cv_head`, `br` 등) 델타 통계 학습
    - 학습 프로파일 저장 후 대상 OTO에 즉시 적용
- 신규 공개 API:
  - 한국어 (`core/oto_generator.py`)
    - `train_kr_autotune_profile`
    - `save_kr_autotune_profile`
    - `load_kr_autotune_profile`
    - `apply_kr_autotune_profile_to_oto`
  - 일본어 (`core/ja_oto_generator.py`)
    - `train_ja_autotune_profile`
    - `save_ja_autotune_profile`
    - `load_ja_autotune_profile`
    - `apply_ja_autotune_profile_to_oto`

## 2-7) 프리셋 자음군 타이밍(치찰/파열, 유성/무성) 보강 + 자동 테스트 추가 (2026-02-27)
- 대상 파일:
  - `core/oto_generator.py`
  - `core/ja_oto_generator.py`
  - `scripts/test_profile_timing_presets.py`
- 한국어 프리셋 보강:
  - 프리셋 적용 후 alias 초성 기준으로 자음군 특성 보정 추가.
  - 분류:
    - manner: `sibilant`, `plosive`, `other`
    - voicing: `voiced`, `voiceless`, `unknown`
  - 보정 대상:
    - `pre`(선행발성)
    - `cons_gap`(`consonant - pre`)
    - `cut_gap`(`abs(cutoff) - consonant`)
    - `ovl_ratio`(`overlap / pre`)
  - 기대 효과:
    - 치찰음: pre/ovl 비율 증가
    - 파열음: pre/cons_gap 단축
    - 유성음: 무성음 대비 pre 소폭 증가
- 일본어 프리셋 보강:
  - `_apply_ja_style_profile`에 alias 텍스트 기반 자음군 보정 추가.
  - `cv/cv_head/vc/vcv` 별로 onset 추출 후 같은 축(manner/voicing) 보정 적용.
  - `_post_adjust_params(..., alias_text=...)` 전달로 실제 생성 루프에서 보정 반영.
- 자동 테스트 스크립트:
  - `scripts/test_profile_timing_presets.py`
  - 검증 범위:
    - KR preset
    - JA preset(`cvvc`, `vcv`)
  - 핵심 assertion:
    - `pre(sibilant) > pre(plosive)`
    - `ovl/pre(sibilant) > ovl/pre(plosive)`
    - `pre(voiced) > pre(voiceless)`
    - `cons_gap(plosive) < cons_gap(sibilant)`

## 2-8) GUI에 '베이스 OTO 없음' 선택 추가 (2026-02-27)
- 대상 파일:
  - `main.py`
- 변경 내용:
  - 템플릿 OTO 입력 영역 아래에 체크박스 추가:
    - `'베이스 OTO 없음' (CVVC/VCV OpenUtau 호환 에일리어스 자동 생성)`
  - 체크 시:
    - 템플릿 입력창/찾아보기 버튼 비활성화
    - OTO 생성 실행 시 `tpl_path`를 강제로 빈 값으로 전달
  - 체크 해제 시:
    - 템플릿 입력창/찾아보기 버튼 재활성화
- 설정 저장/복원:
  - `config.json`에 `no_base_oto` 항목 저장/로드 추가
  - 오류 보고서에도 현재 상태(`베이스 OTO 없음`) 출력 추가

## 2-9) 일본어 VCV 한 음절 밀림 매핑 보강 (2026-02-27)
- 대상 파일:
  - `core/ja_oto_generator.py`
- 문제:
  - VCV alias 일부에서 음절 인덱스를 단순 순서로만 전진시키며, 특정 케이스에서 한 음절 앞/뒤로 밀리는 매핑이 발생.
- 개선:
  - VCV 전용 근거리 매칭 추가:
    - alias의 목표 음절(예: `a kya`의 `kya`) 추출
    - 현재 기대 인덱스 주변(`-2 ~ +3`) 음절 후보와 점수 비교
    - 모음 일치/초성 일치 가중치로 최적 인덱스 선택
  - 보정이 발생하면 로그에 `VCV 음절 정렬 보정 x->y (alias)` 출력
- 기대 효과:
  - “한 음절씩 이르거나 늦는” VCV 오프셋 매핑 오류 감소

## 2-10) 일본어 에일리어스 표기 선택 추가 (2026-02-27)
- 대상 파일:
  - `main.py`
  - `core/ja_oto_generator.py`
- GUI:
  - `JP 에일리어스` 옵션 추가
  - 선택값:
    - `원본 그대로`
    - `히라가나`
    - `로마자`
  - 한국어 모드에서는 해당 옵션 비활성화
- 생성기:
  - `generate_ja_oto(..., alias_style=...)` 파라미터 추가
  - 출력 직전 alias 변환:
    - `original`: 입력 alias 유지
    - `hiragana`: 가나를 히라가나로 통일, 로마자 음절은 가능한 범위에서 히라가나로 변환
    - `romaji`: 가나 alias를 로마자 음절로 변환
  - 한국어 생성기는 기존대로 로마자 기반 출력 유지(변경 없음)
- 설정 저장/복원:
  - `config.json`에 `ja_alias_style` 항목 저장/로드 추가

## 3) 프로젝트 개요
- 목적: UTAU 음원용 `oto.ini`를 MFA(TextGrid) 기반으로 자동 생성.
- 지원 언어:
  - 한국어: CVC/CVVC/VCV/연단음 자동 매핑
  - 일본어: CVVC/VCV/연단음 자동 매핑
- GUI: `CustomTkinter` 기반 단일 실행형 워크플로우.

## 4) 현재 구조
- 진입점: `main.py`
- 핵심 모듈:
  - `core/lab_generator.py`: 한국어 Lab/Dict 생성 + 커스텀 발음 매핑 로더
  - `core/ja_lab_generator.py`: 일본어 Lab/Dict 생성(로마자/가나 파싱 포함)
  - `core/mfa_runner.py`: MFA 탐지/모델 확인/다운로드/정렬 실행/한국어 패치
  - `core/oto_generator.py`: 한국어 OTO 생성
  - `core/ja_oto_generator.py`: 일본어 OTO 생성
  - `core/oto_validator.py`: 생성된 OTO 자동 검증 리포트 생성
  - `core/textio_utils.py`: 인코딩 자동 판독 유틸

## 5) 구현 완료 기능 (코드 확인)

### 5.1 GUI/파이프라인
- 4단계 실행 지원:
  - Lab 생성
  - Dictionary 생성
  - MFA 정렬
  - OTO 생성
- 전체 자동 실행 버튼 + 단계별 개별 실행 버튼 구현.
- 설정 저장/복원(`config.json`) 구현.
- 로그 탭/오류 리포트(`error_report_*.txt`) 생성 기능 구현.

### 5.2 언어/포맷
- 언어 선택 분기(한국어/일본어) 구현.
- 포맷 지정:
  - 자동 감지
  - CVC/연단음
  - CVVC
  - VCV(연속음)
- 템플릿 OTO 미지정 시 자동 템플릿(에일리어스) 생성 구현.

### 5.3 OTO 생성 기능
- OpenUtau 호환 alias 복제 옵션 구현(한국어/일본어 모두).
- `- CV`, `CV -`, 종성 변형/배치음 변형 등 생성 로직 구현.
- alias 접미사 일괄 부여(`_C4` 형태) 구현.
- 단모음 누락 자동 생성 옵션 구현(`gen_missing_vowels`).
- 숨소리(`br`, `R/H`) 처리 분기 구현.
- 파라미터 순서 강제/보정(`validate_oto_params`) 구현.

### 5.4 커스텀 발음 매핑
- GUI에 커스텀 매핑 파일(`.txt`) 입력 필드 구현.
- `key=value` 포맷 로딩 구현 (`load_custom_phonemes`).
- Lab/Dictionary 생성 단계에서 커스텀 매핑 반영 구현.
- 일본어 OTO 생성에서는 alias 분류에 커스텀 맵 반영됨.

### 5.5 MFA 설치/연동
- 앱 내부 MFA 자동 설치 루틴 구현(Conda 감지/포터블 설치 fallback).
- 한국어/일본어 모델 다운로드 및 실행 전 의존성 점검 구현.
- 한국어 `eunjeon` 패치 루틴 구현(`patch_mfa_korean_support`).

### 5.6 검증
- OTO 생성 후 자동 검증 실행(`validate_oto_timing`) 구현.
- 검증 리포트 파일 생성: `*.validation.txt`.
- mel activity + TextGrid 경계 + alias 순서 기반 경고/오류 체크 구현.

## 6) 최근 실행 결과(로그 기준)
- 최신 로그: `logs/log_20260227_001335.txt`
- 확인된 성공:
  - 일본어 Lab 생성: 85/85
  - 일본어 사전 생성: 131 항목
  - MFA 정렬: 성공
  - 일본어 OTO 생성: 완료(출력 경로 기록됨)
- 실행 중 경고:
  - 일본어 tokenizer 의존성 설치 후 import 검증 실패 경고 1회(정렬은 계속 진행되어 성공).
  - 일부 파일은 음소-음절 매핑 실패로 원본 라인 유지 fallback 처리.
- 최신 튜닝 재실행 결과(코드 수정 후 직접 재생성/검증):
  - 일본어 OTO 생성: `85/85`, error `0`
  - 자동 검증: error `0`, warning `2`

## 7) 남은 이슈/기술 부채
- 커스텀 발음 JSON 포맷은 아직 미구현.
  - 현재는 `key=value` 텍스트 포맷만 지원.
- 한국어 OTO 생성(`core/oto_generator.py`)에서 `custom_map`을 로드만 하고 alias 분류/포맷 감지 호출에 전달하지 않음.
  - `classify_alias(alias, custom_map)`/`detect_alias_format(..., custom_map)` 호출 누락.
  - 결과적으로 한국어 OTO 단계의 커스텀 매핑 반영이 부분적일 수 있음.
- 버전 표기 불일치 가능성:
  - `main.py`의 `APP_VERSION`은 `2.0.0`.
  - 문서/체크리스트에는 `v2.3` 언급이 있어 동기화 필요.
- 자동 테스트 체계 부족:
  - 정식 단위/통합 테스트 스위트 없음.
  - `test_auto.py`는 더미 WAV 기반 샘플 스크립트 수준.

## 8) 현재 산출물
- 실행 파일: `UTAU_Auto_OTO.exe`
- 릴리즈 폴더: `UTAU_Auto_OTO_Release/`
- 빌드 스크립트: `build.py`
- MFA 셋업 스크립트: `setup_mfa.bat`

## 9) 개발 재개 시 권장 우선순위
1. 한국어 OTO 경로의 `custom_map` 전달 누락 수정.
2. 커스텀 발음 파일 포맷을 `.json`까지 확장.
3. 버전 표기 정리(`APP_VERSION`, 문서, 릴리즈명 동기화).
4. 최소 회귀 테스트 추가:
   - 한국어/일본어 각각 1개 이상 샘플 세트
   - 커스텀 발음 적용 케이스
   - OpenUtau alias 생성 케이스
5. 일본어 tokenizer import 경고 재현 및 원인 고정.

## 10) 빠른 재시작 체크리스트
- `python -m pip install -r requirements.txt`
- 필요 시 `setup_mfa.bat` 1회 실행
- `python main.py` 또는 `run.bat` 실행
- 테스트용:
  - `python -m compileall main.py core`
  - `python test_auto.py`

## 11) 2026-02-27 CVVC 품질 강화 (VCV2CVVC 원리 반영)
- 참고 레포(`utau_vcv2cvvc`)의 핵심 원리(VCV alias를 VC/CV로 분해, 자음군별 타이밍 보정)를 현재 일본어 생성 로직에 반영.
- `core/ja_oto_generator.py` 개선:
  - `VCV -> (VC/VV) + CV` 전개 유틸 추가: CVVC/CVC 포맷 처리 시 VCV 템플릿을 자동 전개하여 누락 alias를 줄임.
  - 자음군별 VC 브릿지 타이밍 프리셋 추가:
    - 분류: `plosive`, `sibilant`, `nasal`, `liquid`, `glide`, `fricative`, `default`
    - 보정 항목: `offset padding`, `pre lead`, `overlap ratio`, `consonant/cutoff gap`
  - VC 파라미터 계산에서 위 프리셋을 반영하도록 로직 강화(기존 단일 휴리스틱 대비 세분화).
- 로그 추가:
  - 파일 단위 VCV 전개 결과(`원본 줄 수`, `전개 줄 수`, `bridge 추가 수`)를 출력해 디버깅 가능.
- 검증:
  - `python -m py_compile core/ja_oto_generator.py` 통과.
  - 샘플 입력으로 VCV 전개 동작(`a ka -> a k + ka`) 확인.

## 12) 2026-02-27 VCV 역적용 보강 (CVVC 원리 재사용)
- 요청 배경:
  - CVVC에 반영한 `VC+CV 분해 기반 타이밍` 원리를 VCV에도 역으로 적용.
- 구현:
  - `core/ja_oto_generator.py`
  - 신규 함수: `_compute_vcv_params_from_virtual_split(...)`
    - VCV alias를 가상 `VC + CV`로 분해해 offset/pre/ovl/consonant/cutoff를 재조합.
    - 자음군별 브릿지 프리셋(`JA_CVVC_BRIDGE_TIMING`)을 동일하게 사용.
  - VCV 처리 블록에서 기존 고정 휴리스틱 대신 신규 함수 결과를 사용하도록 교체.
- 기대 효과:
  - VCV에서 선행발성/오버랩이 자음군 특성(파열/치찰/비음 등)에 맞춰 안정화.
  - 박자 밀림/당김이 큰 케이스를 줄이고, CVVC와 톤 일관성 확보.
- 검증:
  - `python -m py_compile core/ja_oto_generator.py` 통과.
  - 샘플 파라미터 스모크 테스트로 함수 출력 정상 확인.

## 13) 2026-02-27 앵커+기존 로직 융합(한/일 공통)
- 목표:
  - 기존 휴리스틱 로직을 유지하면서, `TextGrid 앵커(이전 V/현재 C/현재 V)` 계산값과 `base oto shape`를 함께 쓰는 하이브리드로 강화.
- 공통 추가 로직:
  - `base oto` 한 줄에서 상대 타이밍 shape 추출:
    - `pre`, `cons_gap`, `cut_gap`, `ovl_ratio`
  - 앵커 계산 결과와 shape를 alias 타입별 가중치로 블렌딩:
    - 타입: `cv`, `cv_head`, `vc`, `vv`, `vcv`
    - 원칙: 절대 offset 복사 대신 gap/ratio 중심 보정
- 적용 파일:
  - `core/oto_generator.py` (한국어)
  - `core/ja_oto_generator.py` (일본어)
- 기대 효과:
  - CVVC/VCV에서 화자/뱅크별 파라미터 분포를 base oto로 흡수
  - 기존 정렬 앵커 기반 위치 안정성은 유지하면서 과도한 튐 감소
- 검증:
  - `python -m py_compile core/oto_generator.py core/ja_oto_generator.py` 통과
  - `python -m compileall core` 통과

## 14) 2026-02-27 블렌딩 ON/OFF A/B 비교
- 목적:
  - `현재 로직 + 앵커 기반 + base shape 블렌딩`(ON)과 블렌딩 비활성(OFF) 결과를 동일 입력으로 비교.
- 비교 방법:
  - 환경변수 `UTOA_DISABLE_BASE_SHAPE_BLEND=1` 설정 시 OFF, 미설정 시 ON.
  - 동일 보이스뱅크/동일 템플릿/동일 옵션으로 `generate_oto`/`generate_ja_oto` 실행 후 자동 검증(`validate_oto_timing`) 비교.

### 14-1) 일본어 (SODAsoo_VCV_Wasteland)
- 입력:
  - wav/textgrid: `...\SODAsoo_VCV_Wasteland 2025-02-06_1`
  - template: `baseoto.ini`
- 결과:
  - OFF: error `6`, warning `43` (958 lines)
  - ON : error `6`, warning `43` (958 lines)
  - 파라미터 변화: 공통 958줄 중 891줄 값 변경
    - 평균 Δpre `+54.2532`, Δcons `+64.2807`, Δcutoff `-110.5415`, Δovl `+17.6547`

### 14-2) 한국어 (SODAsoo KR CVC, template=oto_8mora.ini)
- 결과:
  - OFF: error `3`, warning `130` (532 lines)
  - ON : error `3`, warning `130` (532 lines)
  - 파라미터 변화: 0줄 (template shape 정보가 거의 없어 블렌딩 영향 없음)

### 14-3) 한국어 (SODAsoo KR CVC, template=oto.ini)
- 결과:
  - OFF: error `3`, warning `901` (3565 lines)
  - ON : error `3`, warning `907` (3565 lines)
  - 파라미터 변화: 공통 2891줄 중 2807줄 값 변경
    - 평균 Δpre `-1.6780`, Δcons `-2.3643`, Δcutoff `+0.6103`, Δovl `-0.9626`

- 산출물:
  - 비교 JSON: `logs/blend_compare_results.json`
  - 검증 리포트:
    - `...\SODAsoo_VCV_Wasteland 2025-02-06_1\oto.compare_blend_off.ini.validation.txt`
    - `...\SODAsoo_VCV_Wasteland 2025-02-06_1\oto.compare_blend_on.ini.validation.txt`
    - `...\SODAsoo KR CVC\oto.compare_blend_off.ini.validation.txt`
    - `...\SODAsoo KR CVC\oto.compare_blend_on.ini.validation.txt`
    - `...\SODAsoo KR CVC\oto.compare_fullbase_blend_off.ini.validation.txt`
    - `...\SODAsoo KR CVC\oto.compare_fullbase_blend_on.ini.validation.txt`

## 15) 2026-02-27 main.py 1차 모듈화 (수정 용이성 개선)
- 목적:
  - `main.py`에 집중된 보조 로직을 분리해 유지보수/수정 난이도를 낮춤.
- 추가 파일:
  - `ui/app_mixins.py`
  - `ui/__init__.py`
- 분리된 책임:
  - `FileDialogMixin`: 폴더/파일 선택, 저장 경로 선택 유틸
  - `AppRuntimeMixin`: 종료 처리, 로그 출력, 경고 팝업, 스레드 실행, 자동 검증, 에러 리포트
  - `ConfigMixin`: `config.json` 저장/불러오기
- main.py 변경:
  - `App` 클래스가 믹스인 기반으로 동작하도록 변경
  - 기존 보조 메서드(로그/설정/파일다이얼로그/종료루틴 등) 본문을 `main.py`에서 제거
  - UI 빌드와 파이프라인 핵심 흐름에 집중하도록 정리
- 검증:
  - `python -m py_compile main.py ui/app_mixins.py core/sofa_runner.py` 통과
- 비고:
  - 동작은 유지하면서 구조만 나누는 보수적 리팩터링(1차).
  - 다음 단계로 탭 UI 구성, 파이프라인 단계 실행 로직도 추가 분리 가능.

## 16) 2026-02-27 main.py 2차 모듈화 (탭 UI/파이프라인 분리)
- 목표:
  - `main.py`를 UI 뼈대/상태 변수 중심으로 축소하고, 실제 동작 로직을 `ui` 모듈로 분리.
- 추가 파일:
  - `ui/tab_builders_mixin.py`
  - `ui/pipeline_actions_mixin.py`
- 분리 내용:
  - `TabBuildersMixin`
    - `_build_pipeline_tab`
    - `_build_params_tab`
    - `_build_log_tab`
    - `_build_profile_tune_tab`
  - `PipelineActionsMixin`
    - SOFA 모델 준비/안내 관련 메서드
    - MFA/SOFA 설치 버튼 동작 메서드
    - Lab/Dictionary/Align/OTO/Profile/Full pipeline 실행 메서드
- main.py 구조 변화:
  - `App` 상속 체인을 다음과 같이 변경:
    - `FileDialogMixin, AppRuntimeMixin, ConfigMixin, TabBuildersMixin, PipelineActionsMixin, ctk.CTk`
  - `main.py`에는 다음만 남김:
    - 앱 초기화(`__init__`)
    - 상단/하단 프레임 배치(`_build_ui`)
    - 언어/토글/파라미터 보조 메서드
- 효과:
  - 기능 수정 시 대상 파일이 명확해짐
  - UI 탭 변경과 파이프라인 로직 변경 충돌 감소
- 검증:
  - `python -m py_compile main.py ui/app_mixins.py ui/tab_builders_mixin.py ui/pipeline_actions_mixin.py core/sofa_runner.py` 통과

## 17) 2026-02-27 main.py 3차 모듈화 (레이아웃/상태 메서드 분리)
- 목표:
  - `main.py`에서 상단/하단 레이아웃 코드와 UI 상태 보조 메서드를 분리해 파일 복잡도 추가 축소.
- 추가 파일:
  - `ui/layout_mixin.py`
- 분리된 메서드:
  - `_build_ui`
  - `_get_language`
  - `_on_language_change`
  - `_get_ja_alias_style_code`
  - `_on_no_base_oto_toggle`
  - `_toggle_advanced_options`
  - `_get_params`
- App 상속 체인:
  - `LayoutMixin, FileDialogMixin, AppRuntimeMixin, ConfigMixin, TabBuildersMixin, PipelineActionsMixin, ctk.CTk`
- 결과:
  - `main.py`는 초기화/엔트리포인트 중심 파일로 축소됨.
  - 기능 수정 시 관심사별 파일(`layout`, `tab_builders`, `pipeline_actions`, `app_mixins`)로 바로 진입 가능.
- 검증:
  - `python -m py_compile main.py ui/layout_mixin.py ui/tab_builders_mixin.py ui/pipeline_actions_mixin.py ui/app_mixins.py` 통과

## 18) 2026-02-27 Align/OTO 로직 분리
- 요청 반영:
  - 정렬(Align)과 OTO 생성 로직만 별도 모듈로 분리.
- 추가 파일:
  - `ui/align_actions_mixin.py`
  - `ui/oto_actions_mixin.py`
- 이동된 메서드:
  - `AlignActionsMixin`: `_run_mfa`
  - `OtoActionsMixin`: `_run_oto_gen`
- App 상속 체인 업데이트:
  - `LayoutMixin, FileDialogMixin, AppRuntimeMixin, ConfigMixin, TabBuildersMixin, AlignActionsMixin, OtoActionsMixin, PipelineActionsMixin, ctk.CTk`
- 기대 효과:
  - 정렬/원음설정 생성 수정 시 변경 범위가 분리되어 추적/테스트가 쉬워짐.
- 검증:
  - `python -m py_compile main.py ui/*.py` 통과

## 19) 2026-02-27 일본어 CVVC VC 타이밍 보강 (vcv2cvvc 원리 반영)
- 배경:
  - 일본어 CVVC에서 VC의 선행발성/오버랩이 뒤로 밀리거나 짧게 잡혀 체감 박자가 늦는 사례가 재발.
- 개선 핵심:
  - `core/ja_oto_generator.py`에 VCV 기반 앵커 계산 함수 추가:
    - `_is_reliable_base_profile`
    - `_compute_vc_params_from_vcv_anchor`
  - 참고 원리:
    - `utau_vcv2cvvc`의 `p1/p2` 상대 이동 방식(원본 VCV 기준)과 TextGrid 경계를 결합.
  - 적용 조건:
    - `VCV -> CVVC/CVC` 전개 상황에서(`detected_format == 'vcv'`) VC 라인에 우선 적용.
    - 베이스 라인이 비정상(0 파라미터 등)인 경우 기존 동적 계산식으로 자동 폴백.
- 기대 효과:
  - VC pre 절대 위치가 과도하게 뒤로 밀리는 현상 완화.
  - VC overlap이 앞 모음 tail 근처에 더 안정적으로 고정.
  - singer별 원본 VCV 상대 타이밍을 더 잘 보존.
- 검증:
  - `python -m py_compile core/ja_oto_generator.py` 통과.

## 20) 2026-02-27 한국어 CVC 이중모음 음절 밀림 매핑 보강
- 배경:
  - 한국어 CVC에서 이중모음(예: `nya`, `ryeo`)이 포함된 음절이 자동 OTO 생성 시 한 칸 앞/뒤로 밀리는 사례가 반복.
- 개선 핵심:
  - `core/oto_generator.py`에 한국어 음절 분해 헬퍼 추가:
    - `_split_kr_syllable_parts` (초성/중성/종성 분리)
    - `_kr_cv_kernel` (비교용 `onset+vowel` 핵심 토큰)
  - CV 매핑 점수 함수 `_cv_match_score`를 종성 포함 음절에도 강건하도록 보강:
    - `nyeong` vs `nyeo`처럼 종성 유무가 달라도 핵심 CV 일치 시 높은 점수 부여.
  - 매핑 단계에서 `roman_cv`를 우선 사용하고, 과도한 전진 점프를 억제:
    - 기대 인덱스 점수가 충분하면 다음 음절로의 불필요한 이동을 막음.
  - 템플릿 미사용 자동 생성 경로도 보강:
    - 이전 음절 받침을 `N/L/M/NG/K/T/P/H`로 안정 매핑하도록 개선.
- 기대 효과:
  - 이중모음 + 받침 조합에서 음절 인덱스 오프셋(한 칸 밀림) 감소.
  - CVC/CVVC 자동 매핑의 연속성 및 재현성 향상.
- 검증:
  - `python -m py_compile core/oto_generator.py` 통과.
  - `python -c "import core.oto_generator as m; print('oto_generator import ok')"` 통과.

## 21) 2026-02-27 정렬 오차(무음 gap 정렬) 안전장치 추가
- 배경:
  - TextGrid 경계가 어긋난 샘플에서 OTO pre(선행발성) 기준점이 발음 사이 빈 공간(gap)에 놓이는 사례 확인.
- 개선 핵심:
  - `core/oto_generator.py`에 무음 gap 보정 후처리 추가:
    - `_nearest_phone_edge_ms`
    - `_surrounding_phone_gap`
    - `_stabilize_params_to_phone_activity`
  - 동작:
    - 계산된 pre 절대위치가 충분히 큰 gap 내부이거나 phone 경계와 멀면,
      alias 타입(`vc/vv` vs `cv/cv_head/vcv`)에 맞춰 근처 유효 phone 경계로 스냅.
    - 이후 `validate_oto_params`로 안전 범위를 재보정.
  - 적용 위치:
    - 일반 CV/CVC/VC 처리 공통 경로에서 최종 write 직전.
- 부가 수정:
  - 이전 패치 중 `syllables_info` 블록 들여쓰기 오동작을 수정해 런타임 안정성 보강.
- 기대 효과:
  - 정렬 품질이 낮은 TextGrid에서도 에일리어스가 완전 무음 구간에 박히는 문제 감소.
- 검증:
  - `python -m py_compile core/oto_generator.py` 통과.
  - `python -c "import core.oto_generator as m; print('oto_generator import ok')"` 통과.

## 22) 2026-02-27 일본어 안전장치 + CVVC/CVC 공통 원리 반영
- 반영 요청:
  - 일본어에도 무음 gap 정렬 안전장치 적용.
  - CVVC(CVC) 생성을 일본어/한국어 공통 원리(VC 연결 안정화, 중복 파열 억제)로 보강.
- 일본어(`core/ja_oto_generator.py`) 변경:
  - `_post_adjust_params` 내부에 gap 인지 스냅 로직 추가:
    - `_nearest_phone_edge_ms`
    - `_surrounding_gap_ms`
  - pre 절대위치가 큰 무음 gap 내부에 있거나 phone 경계와 멀면:
    - `vc/vv`는 다음 onset 직전으로,
    - `cv/cv_head/vcv`는 이전 phone 끝 경계로 스냅.
  - VC 파열/파찰음(`JA_PLOSIVE_CONSONANTS`)은 다음 자음 onset 기준으로 `consonant/cutoff`를 재클램프해 중복 파열을 줄임.
- 한국어(`core/oto_generator.py`) 변경:
  - VC 비파열/파열 공통 계산 뒤, `alias_type == 'vc'`일 때 다음 자음 onset 기준 클램프 추가.
  - 파열/치찰 VC는 onset 직전에서 더 타이트하게 자르고, 비파열은 onset 이후 허용 범위를 좁혀 안정화.
- 참고한 외부 원리 요약:
  - CVVC 작성 가이드의 핵심(VC는 다음 CV와의 접속점 중심, 파열음 중복 회피).
  - SHIRO 문서의 정렬 안정화 관점(검색 공간/경계 오차에 따른 후처리 필요성).
- 검증:
  - `python -m py_compile core/ja_oto_generator.py core/oto_generator.py` 통과.
  - `python -c "import core.ja_oto_generator as ja, core.oto_generator as ko; print('imports ok')"` 통과.

## 23) 2026-02-27 VC 계산 로직 전환 (CV 선계산 기반)
- 요청 반영:
  - `VC`를 직접 단독 추정하는 비중을 줄이고, 먼저 `CV`를 음절 단위로 계산한 뒤
    인접한 두 `CV` 파라미터를 기준으로 사이 `VC`를 계산하도록 강화.
- 한국어(`core/oto_generator.py`):
  - 음절별 CV 앵커 캐시 추가:
    - `_estimate_cv_anchor`
    - `cv_anchor_by_idx`
  - VC 계산 함수 추가:
    - `_compute_vc_from_adjacent_cv`
  - 적용 방식:
    - `VC/VV` 처리 시 이전/다음 음절의 CV 앵커를 우선 참조.
    - 앵커가 부족하거나 비정상이면 기존 동적 계산식으로 폴백.
- 일본어(`core/ja_oto_generator.py`):
  - 음절별 CV 앵커 캐시 추가:
    - `_estimate_ja_cv_anchor`
    - `cv_anchor_by_idx`
  - VC 계산 함수 추가:
    - `_compute_ja_vc_from_adjacent_cv`
  - 적용 방식:
    - 기존 `VCV base anchor`(있을 때) 우선 유지.
    - 그 외 `VC/VV`는 인접 CV 앵커 기반 계산을 우선 적용.
    - 마지막으로 기존 동적 계산식으로 폴백.
- 기대 효과:
  - VC 타이밍이 앞뒤 CV와 더 일관되게 붙어 박자 흔들림 감소.
  - 정렬 오차가 있는 샘플에서도 VC가 과도하게 앞/뒤로 튀는 현상 완화.
- 검증:
  - `python -m py_compile core/oto_generator.py core/ja_oto_generator.py` 통과.
  - `python -c "import core.oto_generator as ko, core.ja_oto_generator as ja; print('imports ok')"` 통과.

## 24) 2026-02-27 CV 기준점 오정렬 대응 (한국어)
- 배경:
  - CV 자체가 잘못된 위치에 박히는 사례가 다수 확인됨.
  - 원인 후보: words tier 경계 불안정 + 모음(핵) 검출 누락.
- 개선 내용:
  - IPA 모음 표기를 확장해 모음 핵 검출 정확도 개선 (`ɯ`, `ʌ`, `ɛ`, `ə` 등).
  - words tier 의존도를 낮추기 위해, alias 시퀀스 기반 CV 타깃 추출 후
    phone nuclei(모음 핵) 기반 음절 재구성 fallback 추가:
    - `_extract_cv_targets_from_lines`
    - `_build_kr_syllables_from_phone_nuclei`
  - 적용 규칙:
    - words 개수와 CV 타깃 개수 불일치가 크면 phone 기반 매핑으로 전환.
    - words tier가 없거나 매핑 실패 시에도 phone 기반 매핑을 재시도.
  - 결과:
    - CV 기준점이 음절 단위로 밀리는 현상 완화.
    - 정렬 불안정 파일에서 “잘못된 음절에 CV 박힘” 감소.
- 검증:
  - `python -m py_compile core/oto_generator.py` 통과.
  - `python -c "import core.oto_generator as m; print('oto_generator import ok')"` 통과.

## 25) 2026-02-27 일본어 CVVC 동일 증상 대응 (CV 기준점 재정렬)
- 배경:
  - 일본어 CVVC에서도 CV 기준점이 잘못된 음절로 밀리는 현상 확인.
- 개선 내용(`core/ja_oto_generator.py`):
  - alias 기반 CV 타깃 추출 추가:
    - `_alias_to_ja_cv_target`
    - `_extract_ja_cv_targets_from_lines`
  - phone nuclei 기반 음절 재구성 추가:
    - `_build_ja_syllables_from_phone_nuclei`
  - `CVVC/CVC` 모드에서는 alias/phone 기반 매핑을 우선 사용하도록 전환.
  - 모음 핵 판별 확장(`ə`, `ɪ`, `ʊ`, `æ`, `ɑ`, `ɔ`, `ɐ` 포함).
- 기대 효과:
  - words/file-name 경계가 흔들려도 CV가 다른 음절에 박히는 현상 완화.
  - 일본어 CVVC의 음절 단위 안정성 향상.
- 검증:
  - `python -m py_compile core/ja_oto_generator.py` 통과.
  - `python -c "import core.ja_oto_generator as m; print('ja_oto_generator import ok')"` 통과.

## 26) 2026-02-27 매핑 정책 수정 (TextGrid 우선 + 이탈 보정)
- 배경:
  - alias/phone 기반 보정을 우선 적용하면 일부 샘플에서 정확도가 오히려 하락.
- 정책 변경:
  - 기본은 TextGrid 기반 매핑(한국어: words, 일본어: filename/words)을 유지.
  - alias/phone 기반 후보는 **보조 보정안**으로만 사용.
  - 점수 기반 조건(`base_score < 54` and `alt_score >= base_score + 12`)을 만족할 때만 보정 적용.
- 구현:
  - 한국어:
    - `_score_kr_syllable_mapping` 추가
    - 매핑 선택 시 `TextGrid(words)` 유지/보정 분기 로그 추가
  - 일본어:
    - `_score_ja_syllable_mapping` 추가
    - 매핑 선택 시 `TextGrid` 유지/보정 분기 로그 추가
- 기대 효과:
  - TextGrid 정렬 정보의 일관성은 유지하면서, 명백한 음절 이탈만 자동 보정.
  - 과보정으로 인한 정확도 저하를 줄임.
- 검증:
  - `python -m py_compile core/oto_generator.py core/ja_oto_generator.py` 통과.
  - `python -c "import core.oto_generator as ko, core.ja_oto_generator as ja; print('imports ok')"` 통과.

## 27) 2026-02-27 CV/VC 품질 보강 2차 (JA/KR)
- 배경:
  - 일본어 CVVC에서 CV 음절 인덱스가 1칸 밀리는 사례, 한국어/일본어 모두에서 TextGrid 매핑 과신으로 인한 위치 오류가 재발.
  - 수동 기준 OTO(`base_oto.ini`)가 있어도 내부 자동 튜닝에 반영되지 않는 케이스 확인.
- 개선 내용:
  - 일본어 CV 인덱스 보정 강화 (`core/ja_oto_generator.py`):
    - `_extract_ja_cv_target_syllable`
    - `_select_ja_cv_syllable_index`
    - `cv_head`, `cv` 처리 시 순차 인덱스 대신 로컬 스코어 기반 보정 적용.
  - TextGrid 보정 임계값 조정 (KR/JA 공통):
    - 기존: `base < 54` and `alt >= base + 12`
    - 변경: `base < 66` and `alt >= 70` and `alt >= base + 8`
    - 의도: TextGrid 우선은 유지하되, 한 음절 밀림 같은 실사용 오류를 더 적극 보정.
  - 프로파일 학습용 OTO 인코딩 보강 (KR/JA 공통):
    - UTF-8, CP932, EUC-KR 등 fallback 디코딩으로 수동 OTO 매칭률 향상.
  - 일본어 숨은 자동 튜닝 레퍼런스 감지 확장:
    - `base_oto.ini`, `oto.base.ini`, `oto_old.ini` 추가 인식.
    - 동일 워크플로우에서 수동 기준 OTO를 내부 학습에 자동 반영.
- 검증:
  - `python -m py_compile core/oto_generator.py core/ja_oto_generator.py` 통과.
  - `Dabi_Test` 샘플 기준:
    - `base_oto.ini` 자동 감지 확인.
    - 자동/수동 매칭 쌍 `273`개, 학습 버킷 `cv/cv_head/vc/vv` 생성 확인.

## 28) 2026-02-27 CV cutoff 과연장(뒷발음 유입) 방지 가드
- 배경:
  - 자동 생성 결과에서 일부 CV 에일리어스가 다음 음절 onset 이후까지 잘려 들어가 뒷 발음이 섞이는 문제 제보.
- 개선 내용:
  - 한국어/일본어 공통으로 CV 계열(`cv`, `cv_head`)에 대해
    **다음 음절 onset 기준 cutoff 상한 가드** 추가.
  - 다음 onset이 파열/치찰 계열이면 더 보수적인 safety margin으로 컷오프를 앞당김.
  - cutoff가 앞당겨지며 `consonant`가 역전되는 경우를 함께 재정렬해 파라미터 일관성 유지.
  - 로그에 보정량 표시:
    - `🛡️ ... CV 컷오프 과연장 보정(-xx.xms) [alias]`
- 구현:
  - 한국어:
    - `_guard_cv_cutoff_to_next_onset` 추가
    - `cv_head` 분기 및 공통 최종 단계에 적용
  - 일본어:
    - `_guard_ja_cv_cutoff_to_next_onset` 추가
    - `cv_head` 분기 및 공통 최종 단계에 적용
- 검증:
  - `python -m py_compile core/oto_generator.py core/ja_oto_generator.py` 통과.
  - `python -c "import core.oto_generator as ko, core.ja_oto_generator as ja; print('imports ok')"` 통과.

## 29) 2026-02-27 한국어 Mel 스펙트로그램 기반 후처리 보정 추가
- 배경:
  - TextGrid 경계 보정만으로는 일부 CV 라인에서 cutoff가 늦어지는 케이스가 남아 있었음.
  - 사용자 요청: wav의 Mel 스펙트로그램 정보와 OTO를 동시에 비교해 보정 강화.
- 개선 내용 (`core/oto_generator.py`):
  - WAV를 모노로 읽어 프레임 단위 Mel 에너지 엔벨로프를 계산하는 내부 유틸 추가:
    - `_read_wav_mono_np`
    - `_mel_envelope`
  - 생성 완료 후 한국어 OTO에 멜 기반 후처리 자동 적용:
    - `_apply_kr_mel_refine_to_oto_file`
  - 보정 규칙:
    - `cv/cv_head` 라인에서 현재 cutoff 이전 구간의 멜 에너지 골짜기(valley)를 탐색.
    - valley 대비 현재 cutoff 지점 에너지가 충분히 높으면, cutoff를 valley 쪽으로 앞당김.
    - `consonant`/`pre` 순서가 깨지지 않도록 동시 재정렬.
  - 로그:
    - `[KR-Mel] 멜 에너지 기반 cutoff 보정 적용: N lines`
  - 비활성화 옵션:
    - 환경변수 `UTOA_DISABLE_MEL_REFINER=1`
- 검증:
  - `python -m py_compile core/oto_generator.py` 통과.
  - `import core.oto_generator` 통과.
  - 샘플(`Dabi_Test/oto_AutoTest2.ini` 복사본) 적용 시:
    - 보정 적용 라인: `36`
    - `cv/cv_head` cutoff 과연장(기준 대비 +40ms 초과) 건수: `34 -> 27` 감소.

## 30) 2026-02-27 일본어 Mel 후처리 + 일본어 보정치 프리셋 하드코딩 갱신
- 배경:
  - 사용자 실행 환경에서도 멜 기반 분석이 자동 적용되길 원함.
  - 일본어 VCV/CVVC 기준 OTO를 외부 파일 의존 없이 추상 통계로 내장(하드코딩)할 필요가 있었음.
- 개선 내용:
  - 일본어 생성 완료 후 멜 에너지 기반 cutoff 보정 자동 적용:
    - `core/ja_oto_generator.py`
    - `_apply_ja_mel_refine_to_oto_file` 추가
    - 생성 종료 단계에서 기본 실행 (`[JA-Mel] ...` 로그 출력)
  - 일본어 프리셋 수치 갱신(하드코딩):
    - `core/oto_profile_presets.py`
    - `ja_vcv_reference_v2`, `ja_cvvc_reference_v2`
    - alias bucket(`cv/cv_head/vc/vv`)별 `pre/cons_gap/cut_gap/ovl_ratio/head_offset`를
      익명화된 통계값으로 갱신.
- 정책:
  - 실사용자 기본 경로에서 멜 보정 활성화.
  - 필요 시 환경변수 `UTOA_DISABLE_MEL_REFINER=1`로 비활성화 가능.
- 검증:
  - `python -m py_compile core/oto_profile_presets.py core/ja_oto_generator.py core/oto_generator.py` 통과.
  - 일본어 멜 보정 함수 단독 실행 확인:
    - 테스트 복사본 기준 `changed 39` 라인 보정.

## 31) 2026-02-27 dB 무음 임계값 + 저가중치 F0 보조 신호 적용
- 배경:
  - 단순 mel valley 기준만으로는 무음 구간 판정이 불안정한 케이스가 존재.
  - 사용자 요청에 따라 “소리 없는 구간” 판정 강화 + F0는 낮은 가중치로 보조 반영.
- 개선 내용:
  - `core/oto_generator.py`:
    - `_estimate_f0_voicing_strength` 추가 (자기상관 기반 간이 유성도 0~1).
    - `_mel_envelope` 확장:
      - `db_db`(프레임 RMS dB)
      - `db_silence_th`(적응형 무음 임계값)
      - `f0_voicing`(저밀도 추정 + 스무딩)
    - KR mel 후처리에서 valley 선택식을 `mel + dB + F0` 결합으로 변경.
      - F0 가중치: `-0.08` (낮은 보조 비중)
  - `core/ja_oto_generator.py`:
    - JA mel 후처리도 동일한 `mel + dB + F0` 선택식으로 갱신.
      - F0 가중치: `-0.08` (KR과 동일)
- 보정 원칙:
  - 주축: mel/dB(무음/저에너지 구간)
  - 보조: F0(유성도 높은 구간은 valley 후보에서 약하게 감점)
- 검증:
  - `python -m py_compile core/oto_generator.py core/ja_oto_generator.py` 통과.
  - 샘플(`Dabi_Test/oto_AutoTest2.ini` 복사본) 기준:
    - KR mel+db+f0 보정 `changed 27`
    - JA mel+db+f0 보정 `changed 29`

## 32) 2026-02-27 JA CVVC voiced/nasal CV stability (m focus)
- Goal:
  - Reduce misplacement of voiced/nasal CV rows in Japanese CVVC, especially `m`.
- Changes:
  - `core/ja_oto_generator.py`
    - Added onset-class-aware scoring in `_select_ja_cv_syllable_index`.
    - Added extra penalties for onset-class mismatch (nasal vs non-nasal, voiced vs voiceless).
    - Increased hold tendency near expected index for voiced/nasal targets.
  - `core/oto_generator.py`
    - Extended `_apply_soft_mel_offset_cutoff_guard(...)` with `onset_hint` and `alias_text`.
    - For low-energy voiced/nasal onsets (`m` etc.), early offset soft-guard is now conservative to avoid pulling offset into vowel onset.
  - `core/ja_oto_generator.py`
    - Wired `onset_hint_local` into JA soft-guard callsites.
- Validation:
  - `python -m py_compile core/oto_generator.py core/ja_oto_generator.py` passed.
  - `python scripts/test_profile_timing_presets.py` passed.

## 33) 2026-02-27 MFA Korean dependency failure handling hardening
- Root cause observed:
  - MFA align failed with ImportError in tokenization stage:
    `Please install Korean support via pip install eunjeon jamo`.
- Fixes applied (`core/mfa_runner.py`):
  - `ensure_korean_support(...)`
    - Added strict install+verify flow for `eunjeon`, `jamo`.
    - Tries multiple install commands (`python -m pip`, `pip.exe`, `conda run ... python -m pip`).
    - Verifies imports after each attempt.
    - Returns `False` when dependency prep still fails (no silent pass-through).
  - `download_mfa_model(...)`
    - Korean model download now aborts early if Korean deps are not prepared.
  - `run_mfa_align(...)`
    - Korean/Japanese dependency check failure now stops alignment immediately with clear error.
    - On non-zero MFA exit, parses tail logs and surfaces dependency-specific error when detected.
- Validation:
  - `python -m py_compile core/mfa_runner.py` passed.
- Additional hardening:
  - Auto-detect `pyexpat`/`libexpat` DLL import issues in `.env` and attempt `libexpat` repair via conda before failing.

## 34) 2026-02-27 MFA 한국어 의존성 설치 실패 대응 보강
- 현상:
  - `Missing Korean tokenizer dependencies (eunjeon, jamo)` 오류가 반복 발생.
  - 원인 중 하나로 `pyexpat/libexpat DLL` 손상 + conda `libmamba` solver 로딩 실패가 확인됨.
- 조치 (`core/mfa_runner.py`):
  - 한국어 의존성 준비 단계에서 `pyexpat/libexpat` DLL 이슈 감지 시 자동 복구 시도.
  - 복구/설치용 conda 명령에 `--solver classic` 강제 적용.
  - 의존성 준비 실패 시 MFA 정렬을 즉시 중단하고 명확한 오류를 반환.
- 검증:
  - `python -m py_compile core/mfa_runner.py` 통과.
## 35) 2026-02-27 한국어 CVC 받침 컷오프/된소리 선행발음 보정
- 목적:
  - 한국어 CVC/CVVC에서 받침 파열 계열(특히 T/P 계열) VC 컷오프가 늦게 잡히는 문제를 완화.
  - 된소리(쌍자음) CV의 선행발음/오버랩으로 인한 어색한 발음 구간을 줄이기.
- 변경 파일:
  - `core/oto_generator.py`
- 핵심 변경:
  - VC 받침 파열 분기 보정 강화
    - T/P 계열을 K 계열보다 더 짧고 이른 컷오프로 계산하도록 분기 추가.
    - `tail_room` 기반 `consonant/cutoff` 배율과 clamp 범위를 축소해 과도한 후행 포함을 방지.
    - 다음 onset 기준 soft cap을 추가해 컷오프가 다음 음절 쪽으로 과도하게 넘어가지 않도록 제한.
  - 된소리 CV 타이밍 보정
    - CV/CV_HEAD/이중모음 CV/앵커 추정에서 된소리의 pre를 약간 더 이르게(오프셋 앞당김) 조정.
    - 된소리의 overlap 상한을 재조정하고 consonant 길이 계산을 소폭 보강.
  - 프로파일 후처리 보정 강화
    - `_apply_kr_consonant_timing_shaping`에서 된소리 onset과 받침 파열 alias(T/P/K) 특성을 반영해
      `pre/cons_gap/cut_gap/ovl` 미세 보정.
- 검증:
  - `python -m py_compile core/oto_generator.py` 통과.
  - `PYTHONUTF8=1 python test_auto.py` 스모크 테스트 통과.
## 36) 2026-02-27 한국어 CV 오프셋 과도 선행(앞 발음 유입) 보정
- 문제:
  - CV/CV_HEAD에서 오프셋이 너무 앞에 잡혀 앞 음절 발음이 섞여 들어가는 사례가 다수 발생.
- 조치 파일:
  - `core/oto_generator.py`
- 개선 내용:
  - `_stabilize_params_to_phone_activity(...)`의 gap 스냅 로직 개선.
    - 기존: CV 계열도 gap 보정 시 `prev_end`로 당겨져 오프셋이 앞당겨질 수 있었음.
    - 변경: `cv/cv_head`는 `next_start - 4ms` 기준으로 스냅하도록 변경해 앞 발음 유입을 억제.
  - CV/CV_HEAD/이중모음 CV의 오프셋 리드(선행 여유) 상수 축소.
    - 기본/파열/유성(sonorant)/된소리 모두 과도한 선행값을 줄여 오프셋을 전반적으로 뒤로 이동.
    - 이중모음 CV의 `target_pre` 범위도 축소하여 과도 선행을 완화.
  - 내부 VC 앵커 추정용 CV 계산(`_estimate_cv_anchor`)에도 동일 기준을 반영해 일관성 유지.
- 검증:
  - `python -m py_compile core/oto_generator.py` 통과.
  - `python scripts/test_profile_timing_presets.py` 통과.
  - `PYTHONUTF8=1 python test_auto.py` 스모크 테스트 통과.
## 37) 2026-02-27 OTO 자동 지정 핵심 경로 리팩토링(기능 보존)
- 목표:
  - 동작 변경 없이 `core/oto_generator.py`의 중복 계산을 줄여 유지보수성을 개선.
  - 특히 한국어 CV 타이밍 계산 경로를 공통화해 추후 튜닝 시 수정 지점을 단일화.
- 변경 파일:
  - `core/oto_generator.py`
- 리팩토링 내용:
  - KR CV 계산 공통 함수 추출:
    - `_compute_kr_cv_timing(...)` 신설
    - 이중모음 CV/일반 CV의 offset, pre, overlap, consonant, cutoff 계산을 공통화.
  - VV 판별 조건 함수 추출:
    - `_looks_like_vv_alias(...)` 신설
    - 기존의 긴 인라인 조건식을 함수로 분리.
  - 호출부 치환:
    - 내부 앵커 추정(`_estimate_cv_anchor`)의 중복 계산 블록을 공통 함수 호출로 대체.
    - 메인 생성 경로의 KR CV 분기(이중모음/일반)도 공통 함수 호출로 대체.
- 보존 원칙:
  - 기존 수식/상수는 유지하고 위치만 이동(구조 리팩토링).
  - VV 별도 경로와 VC/VCV 계산 흐름은 변경하지 않음.
- 검증:
  - `python -m py_compile core/oto_generator.py` 통과.
  - `python scripts/test_profile_timing_presets.py` 통과.
  - `PYTHONUTF8=1 python test_auto.py` 스모크 테스트 통과.
## 38) 2026-02-27 OTO 생성 2차 리팩토링(중첩 함수 외부 분리)
- 목표:
  - `generate_oto()` 내부 중첩 함수를 모듈 상단 순수 함수로 분리해 가독성과 테스트 용이성을 향상.
  - 기능 동작(oto 자동 지정 결과)은 그대로 유지.
- 변경 파일:
  - `core/oto_generator.py`
- 리팩토링 내용:
  - 중첩 함수 외부 분리:
    - `_estimate_cv_anchor_from_syllable(...)` 신설
    - `_compute_vc_from_adjacent_cv(...)` 신설
    - `_guard_cv_cutoff_to_next_onset(...)` 신설
  - `generate_oto()` 내부에서는 위 함수들을 호출만 하도록 단순화.
  - KR CV anchor 추정/VC 인접 CV 기반 계산/다음 onset 침범 가드 로직의 계산식은 유지.
- 기대 효과:
  - `generate_oto()` 본문 길이 감소 및 분기 추적 용이.
  - 후속 튜닝 시 변경 지점 분리로 회귀 위험 감소.
- 검증:
  - `python -m py_compile core/oto_generator.py` 통과.
  - `python scripts/test_profile_timing_presets.py` 통과.
  - `PYTHONUTF8=1 python test_auto.py` 스모크 테스트 통과.
## 39) 2026-02-27 OTO 생성 3차 리팩토링(전략 함수 추가)
- 목표:
  - `generate_oto()`의 대형 분기를 더 작은 전략 함수로 분해해 수정/검증 효율을 높임.
  - 자동 OTO 지정 동작은 유지.
- 변경 파일:
  - `core/oto_generator.py`
- 리팩토링 내용:
  - VCV 분기 전략 함수 추가:
    - `_prepare_vcv_syllable_timing(...)`
    - 음절 인덱스 갱신(`current_w_idx`, `cv_seq_idx`) + 기본 timing 계산을 분리.
  - CV_HEAD 분기 전략 함수 추가:
    - `_prepare_cv_head_syllable_timing(...)`
    - 기존 CV_HEAD 계산식을 함수로 분리.
  - 출력 라인 작성 공통화:
    - `_append_alias_rows(...)`
    - OpenUtau alias 변형 생성/접미사 적용/라인 append 중복 제거.
  - 기존에 분리한 가드/앵커 함수와 결합:
    - `_estimate_cv_anchor_from_syllable(...)`
    - `_compute_vc_from_adjacent_cv(...)`
    - `_guard_cv_cutoff_to_next_onset(...)`
- 효과:
  - `generate_oto()` 본문의 중복 코드 감소.
  - 분기별 튜닝 시 수정 범위 축소.
  - 회귀 테스트 범위(함수 단위) 명확화.
- 검증:
  - `python -m py_compile core/oto_generator.py` 통과.
  - `python scripts/test_profile_timing_presets.py` 통과.
  - `PYTHONUTF8=1 python test_auto.py` 스모크 테스트 통과.
## 40) 2026-02-27 OTO 생성 4차 리팩토링(VC/CV 본문 전략 분해)
- 목표:
  - `generate_oto()`의 메인 분기에서 인덱스 선택/경계 계산/VC 코어 계산을 함수화.
  - 자동 OTO 지정 품질은 유지하면서 수정 난이도와 회귀 위험을 낮춤.
- 변경 파일:
  - `core/oto_generator.py`
- 리팩토링 내용:
  - 인덱스 선택 전략 분리:
    - `_resolve_cv_syllable_index(...)`
    - CV alias와 words/roman 음절 매핑 판단 로직 분리.
  - 경계 계산 전략 분리:
    - `_prepare_cv_bounds_from_syllable(...)`
    - `_prepare_vc_bounds_from_context(...)`
    - CV와 VC 각각의 `c_start/c_end/n_start/n_end` 산출 책임 분리.
  - VC 계산 코어 분리:
    - `_compute_kr_vc_timing(...)`
    - 종성 파열 VC 특수 처리, 인접 CV anchor 기반 처리, 일반 VC fallback을 단일 함수로 통합.
  - 기존 본문 변경:
    - `generate_oto()` 내 `if not is_vc`/`if is_vc` 대형 블록을 위 전략 함수 호출로 치환.
- 효과:
  - 메인 루프의 제어 흐름이 단순해져 튜닝 지점 파악이 쉬워짐.
  - VC 관련 보정 작업 시 수정 범위를 `_compute_kr_vc_timing(...)`으로 한정 가능.
- 검증:
  - `python -m py_compile core/oto_generator.py` 통과.
  - `python scripts/test_profile_timing_presets.py` 통과.
  - `PYTHONUTF8=1 python test_auto.py` 스모크 테스트 통과.
## 41) 2026-02-27 OTO 생성 5차 리팩토링(post-guard 파이프라인 공통화)
- 목표:
  - 분기마다 중복되던 후처리 가드 로직(soft mel/base shape/stabilize/cutoff guard)을 공통 파이프라인으로 통합.
  - 분기별 동작 차이(예: VCV/CV_HEAD에서 stabilize 적용 여부)는 옵션으로 그대로 보존.
- 변경 파일:
  - `core/oto_generator.py`
- 리팩토링 내용:
  - 후처리 공통 함수 추가:
    - `_apply_post_timing_pipeline(...)`
    - soft mel guard, base shape blend, stabilize, cutoff guard, validate를 일관 처리.
  - 로그 공통 함수 추가:
    - `_log_post_timing_events(...)`
    - soft guard 이동량/컷오프 과연장 보정 로그를 공통 처리.
  - 적용 지점:
    - `is_vcv` 분기: 파이프라인 호출(기존과 동일하게 stabilize/cutoff guard 비활성).
    - `is_cv_head` 분기: 파이프라인 호출(기존과 동일하게 stabilize 비활성, cutoff guard 활성).
    - 일반 본문 분기: 파이프라인 호출(기존과 동일하게 stabilize/cutoff guard 활성).
- 검증:
  - `python -m py_compile core/oto_generator.py` 통과.
  - `python scripts/test_profile_timing_presets.py` 통과.
  - `PYTHONUTF8=1 python test_auto.py` 스모크 테스트 통과.
## 42) 2026-02-27 Windows DLL 누락 시스템 팝업 억제
- 문제:
  - FFmpeg/torchcodec 로드 시 일부 환경에서 "누락 DLL" Windows 시스템 팝업이 표시되지만, 실행은 계속되는 케이스가 발생.
- 변경 파일:
  - `main.py`
- 조치:
  - 앱 시작 초기에 `_suppress_windows_loader_popup()` 추가.
  - `SetErrorMode`/`SetThreadErrorMode`로 DLL 로더 관련 시스템 오류 대화상자를 억제.
  - 오류 자체는 예외/로그 경로로 남기고, 사용자 클릭이 필요한 시스템 팝업만 제거.
- 검증:
  - `python -m py_compile main.py` 통과.
  - `PYTHONUTF8=1 python test_auto.py` 통과.
## 43) 2026-02-27 KR/JA CV 한 음절 매핑 오차 억제 보강
- 문제:
  - 한국어/일본어 모두 CV alias가 한 음절 앞/뒤로 밀려 매핑되는 사례가 잔존.
- 변경 파일:
  - `core/oto_generator.py`
  - `core/ja_oto_generator.py`
- 개선 내용:
  - KR (`_resolve_cv_syllable_index`):
    - `best_score - expected_score`(점수 이득) 기준의 점프 허용 임계값 추가.
    - 한 칸 점프(`±1`)는 충분한 이득이 없으면 expected index 유지.
    - expected가 target vowel과 일치하고 best가 불일치인 경우 보수적으로 hold.
    - 뒤로 가는 매핑도 이득이 충분하지 않으면 방지.
  - JA (`_select_ja_cv_syllable_index`):
    - 동일한 점수 이득 기반 hold 가드 추가.
    - 비음/유성 onset 클래스는 더 보수적으로 한 칸 점프 억제.
    - target-expected vowel 일치 + target-best 불일치 상황에서 점프 억제.
- 의도:
  - 정답 확신이 높지 않은 상황에서 index jump를 억제해 “한 음절씩 밀림”을 줄임.
  - TextGrid 기반 정렬 결과를 우선하되, 불확실한 alias 점수로 인한 오매핑을 완화.
- 검증:
  - `python -m py_compile core/oto_generator.py core/ja_oto_generator.py` 통과.
  - `python scripts/test_profile_timing_presets.py` 통과.
  - `PYTHONUTF8=1 python test_auto.py` 스모크 테스트 통과.

## 44) 2026-02-27 CV 오프셋 조기 배치(한 음절 앞 유입) 보정 2차
- 목적
  - 일본어/한국어에서 CV 오프셋이 너무 이르게 잡혀 앞 음절 소리가 섞이는 문제를 줄입니다.
  - CV 매핑이 ±1 음절로 흔들리는 케이스를 더 보수적으로 억제합니다.
- 변경 파일
  - `core/ja_oto_generator.py`
  - `core/oto_generator.py`
- 주요 수정
  - 일본어
    - `CV/cv_head` 후처리 gap 스냅 시, 기존 `prev_end` 기준 대신 `next_start` 직전 기준을 사용하도록 변경.
    - `CV/cv_head` 오프셋 계산을 고정 `c_start-50`에서 onset 계열 기반 가변 리드로 변경.
    - `pre` 상한을 onset 계열/자음 길이에 맞춰 제한해 과도한 선행발성 길이를 방지.
    - `_select_ja_cv_syllable_index` 점프 허용 임계값 강화(특히 비음/유성 계열).
  - 한국어
    - `_compute_kr_cv_timing`의 CV 리드량을 축소하고 `pre` 목표치를 자음 길이 기반으로 재계산.
    - `_prepare_cv_head_syllable_timing`도 동일한 방식으로 리드량 축소/상한 제한.
    - `_resolve_cv_syllable_index`의 ±1 점프/역방향 점프 허용 임계값을 상향해 한 음절 밀림 억제.
- 기대 효과
  - 어두 CV 포함 전반적인 CV 오프셋이 뒤로 정렬되어 앞 발음 유입 감소.
  - `m/n/r` 등 유성·비음 계열에서 발생하던 한 음절 밀림 빈도 감소.
- 검증
  - `python -m py_compile Auto_OTO/core/oto_generator.py Auto_OTO/core/ja_oto_generator.py`
  - `python Auto_OTO/scripts/test_profile_timing_presets.py`
  - `PYTHONUTF8=1 python Auto_OTO/test_auto.py`
  - 모두 통과.

## 45) 2026-02-27 CV 한 음절 오매핑(역방향/과점프) 추가 억제
- 배경
  - 사용자 제보 이미지 기준으로 일본어 CV(`て`, `に`)에서 이전 음절로 당겨지거나(+pre 과대), 한 음절 이상 점프하는 사례가 재확인됨.
- 변경 파일
  - `core/ja_oto_generator.py`
  - `core/oto_generator.py`
- 수정 내용
  - 일본어 CV 매핑(`_select_ja_cv_syllable_index`)
    - 탐색 범위 축소: `e+5 -> e+4`.
    - 점수 계산에 모음 불일치 강한 페널티 추가(`-24`).
    - onset 첫 자모 불일치 페널티 추가(`-9`).
    - `+2` 이상 과점프는 `expected_score`/`best_gain` 조건을 만족할 때만 허용.
    - 역방향 점프(`best_idx < expected`)는 사실상 금지(매우 강한 예외 조건만 허용).
  - 한국어 CV 매핑(`_resolve_cv_syllable_index`)
    - 탐색 범위 축소: `cv_seq_idx+5 -> +4`.
    - `+2` 이상 과점프 보수화.
    - 역방향 점프 허용 조건 강화.
- 기대 효과
  - CV alias가 이전 음절로 미끄러져 offset이 과하게 앞당겨지는 현상 감소.
  - 반복 음절/유사 onset 연속 구간에서 한 음절 오매핑 빈도 감소.
- 검증
  - `python -m py_compile Auto_OTO/core/oto_generator.py Auto_OTO/core/ja_oto_generator.py`
  - `python Auto_OTO/scripts/test_profile_timing_presets.py`
  - `PYTHONUTF8=1 python Auto_OTO/test_auto.py`
  - 모두 통과.

## 46) 2026-02-27 일본어 CV plain/축약행(て vs てぃ) 오매핑 보정
- 배경
  - `て`가 한 음절 앞(축약행/이중모음 행)으로 붙는 사례가 남아 있었음.
  - 파라미터 형상은 맞지만 인덱스가 1칸 앞에 걸리는 문제가 핵심.
- 변경 파일
  - `core/ja_oto_generator.py`
- 핵심 수정
  - `CV` 매핑 함수(`_select_ja_cv_syllable_index`)에 tail(핵심 CV 뒤 잔여 문자열) 비교를 추가.
    - plain CV(target tail 없음) vs 축약/확장 후보(candidate tail 있음) 강한 페널티.
    - onset/vowel 동일해도 `te -> tei` 오인식이 쉽게 선택되지 않도록 조정.
  - `best_idx == expected`일 때 다음 칸(+1)이 명확히 더 적합하면 보정 허용.
    - 조건: target은 plain, expected는 tail 있음, next는 tail 없음, next 점수 우세.
  - 관련 유틸 추가
    - `_ja_syllable_tail(...)`
- 기대 효과
  - `て`/`に` 같은 plain CV가 앞 음절의 축약행/유사행으로 붙는 문제 감소.
  - 한 음절 오프셋 오차의 잔여 케이스를 정밀하게 줄임.
- 검증
  - `python -m py_compile Auto_OTO/core/ja_oto_generator.py Auto_OTO/core/oto_generator.py`
  - `python Auto_OTO/scripts/test_profile_timing_presets.py`
  - `PYTHONUTF8=1 python Auto_OTO/test_auto.py`
  - 모두 통과.

## 47) 2026-02-27 일본어 CVVC(카나 alias) CV 위치 오차 추가 보정
- 배경
  - `に`/`て`가 앞 음절로 붙는 케이스가 남아 있었고, 특히 카나 alias에서 onset 분류가 로마자 기준으로 동작하지 않아 보정 규칙이 비활성화됨.
- 변경 파일
  - `core/ja_oto_generator.py`
  - `core/oto_generator.py`
- 수정 사항
  - 파일명 음절 분할(`_build_syllables_from_filename`) 개선:
    - 기존 균등 샘플링 대신 `목표 모음열`↔`관측 nucleus 모음열` 단조 정렬(DP) 적용.
    - `てぃ/とぅ` 같은 음절열에서 nuclei 과/부족 시 1칸 밀림을 완화.
  - 일본어 CV 매핑 안전장치 강화:
    - CV/CV_HEAD에서 역방향 매핑이 나오면 `expected_idx` 이하로 내려가지 않도록 고정.
  - 카나 alias onset 정규화 보정:
    - `_ja_cv_onset_class`가 카나 alias도 로마자 onset(`t`, `n` 등)으로 해석하도록 수정.
  - 장모음 구간 CV 기준점 지연(조건부):
    - `짧은 C + 매우 긴 V`(예: `て` 케이스)에서 offset 절대 위치를 모음 안정구간 쪽으로 늦추는 late-shift 적용.
    - pre 길이는 유지해 음색 형태는 보존하고 위치만 이동.
- 샘플 확인(동일 데이터 재생성)
  - `_たてぃとぅてとたんた.wav=て`: `1960.00 -> 2329.50`
  - `_なにぬねのなんな.wav=に`: `1030.00 -> 1456.00`
- 검증
  - `python -m py_compile Auto_OTO/core/ja_oto_generator.py Auto_OTO/core/oto_generator.py`
  - `python Auto_OTO/scripts/test_profile_timing_presets.py`
  - `PYTHONUTF8=1 python Auto_OTO/test_auto.py`
  - 모두 통과.

## 48) 2026-02-27 일본어 CV/CV_HEAD 한 음절 밀림(よ, -そ, -と 등) 보정 강화
- 배경
  - 사용자 제보: 일본어 CV/CV_HEAD에서 일부 에일리어스가 뒤 음절(또는 공백 근처)로 붙는 현상.
  - 특히 `よ -> や` 밀림, `- しょ / - も / - そ / - と` 위치 불안정 사례 확인.
- 수정 파일
  - `Auto_OTO/core/ja_oto_generator.py`
- 변경 내용
  - `cv_head` 절대 `head_offset` 블렌딩을 파일 초반/근접 구간에서만 약하게 허용하도록 제한.
    - 후반 음절까지 절대 오프셋으로 끌어당겨지는 문제 방지.
  - `_select_ja_cv_syllable_index` 전진 점프 차단 강화.
    - 기대 음절이 이미 목표 모음을 만족할 때, 모음 불일치 전진 점프를 즉시 차단.
  - 파일명 우선 음절 매핑을 `alias_based`로 다시 덮어쓰는 보정 경로를 제한.
    - `filename_based`가 성공한 경우에는 덮어쓰기 비활성화(정렬 결과 우선).
  - CV/CV_HEAD 모음 불일치 안전장치 추가.
    - `_find_ja_cv_vowel_match_index` 신규 도입.
    - 선택된 음절의 모음이 목표 모음과 다르면 기대 인덱스 주변에서 모음 일치 음절 재탐색 후 보정.
- 확인 결과(샘플)
  - `_やいぃゆいぇよやんや.wav=よ`:
    - 수정 전: `offset=3266.00, pre_abs=3405.67` (뒤 음절 쪽)
    - 수정 후: `offset=2806.00, pre_abs=2925.15` (요 음절 쪽으로 복귀)
  - `- しょ / - も / - そ / - と` 계열도 offset/pre 절대 위치가 공백에서 음절 경계 쪽으로 개선됨.
- 검증
  - `python -m py_compile Auto_OTO/core/ja_oto_generator.py`
  - `generate_ja_oto(...)` 실데이터 재생성 완료 (`processed 85/85`, `errs 0`)

## 49) 2026-02-27 한국어 매핑 보정 2종 우선 적용
- 요청 반영
  - 1) words/filename 기반 매핑 신뢰 구간에서 `alias_based` 덮어쓰기 제한
  - 2) CV 매핑 후 목표 모음 불일치 시 근방 재탐색 보정
- 수정 파일
  - `Auto_OTO/core/oto_generator.py`
- 변경 사항
  - `used_words_based` 플래그 도입
    - words 티어 기반 음절 매핑이 존재하고 `base_score >= 58`인 경우에는 alias 기반 교체를 막아
      TextGrid(words) 결과를 우선 유지.
  - CV 선택 안전장치 강화
    - `_resolve_cv_syllable_index`에서 "기대 음절 모음 일치 + 후보 모음 불일치 + 전진 점프"를 즉시 차단.
    - `_find_kr_cv_vowel_match_index(...)` 신규 추가:
      - CV 선택 이후 현재 음절 모음이 target과 다르면 기대 인덱스 근방(`-1 ~ +2`)에서
        모음 일치 후보를 재탐색하여 보정.
      - 보정 시 로그: `CV 모음 불일치 보정 ...`
- 기대 효과
  - words 정렬이 정상인 파일에서 alias 기반 과보정으로 인한 한 음절 밀림 감소.
  - 이중모음/반모음 계열에서 CV가 인접 음절로 붙는 케이스 완화.
- 검증
  - `python -m py_compile Auto_OTO/core/oto_generator.py Auto_OTO/core/ja_oto_generator.py`
  - `python Auto_OTO/scripts/test_profile_timing_presets.py` (PASS)
  - `$env:PYTHONUTF8='1'; python Auto_OTO/test_auto.py` (PASS)

## 50) 2026-02-27 어두 `-CV` 공백 과포함(오프셋 과선행) 가드 추가
- 배경
  - 어두 `- CV` 에일리어스에서 offset이 현재 음절 onset보다 과도하게 앞서 공백 영역을 많이 포함하는 사례가 반복됨.
- 수정 파일
  - `Auto_OTO/core/oto_generator.py`
  - `Auto_OTO/core/ja_oto_generator.py`
- 변경 사항
  - 한국어: `_guard_cv_head_offset_to_current_onset(...)` 추가
    - 현재 음절 onset(`c_start`) 기준 허용 리드(ms) 상한을 두고 offset floor를 적용.
    - 파열/치찰, 공명음 등 onset 특성에 따라 리드 상한을 다르게 적용.
    - 과선행일 때는 offset만 앞으로 당기지 않고, `pre/cons/cutoff` 절대 기준점을 보존하는 형태로 재계산.
  - 일본어: `_guard_ja_cv_head_offset_to_onset(...)` 추가
    - 동일한 원리로 `cv_head` 오프셋 과선행을 제한.
  - 두 언어 모두 `cv_head` 분기에서 post-adjust 이후 해당 가드를 호출하고 로그 출력.
    - 로그: `CV_HEAD 오프셋 과선행 보정(+xx.xms)`
- 기대 효과
  - 어두 `-CV`에서 불필요한 앞 공백 포함 감소.
  - 선행발성/오버랩의 상대 형태는 유지하면서 절대 위치만 안전하게 정렬.
- 검증
  - `python -m py_compile Auto_OTO/core/oto_generator.py Auto_OTO/core/ja_oto_generator.py`
  - `python Auto_OTO/scripts/test_profile_timing_presets.py` (PASS)
  - `$env:PYTHONUTF8='1'; python Auto_OTO/test_auto.py` (PASS)

## 51) 2026-02-27 어두 `-CV` 길이 단축(컷오프 상대길이 축소) 보정
- 배경
  - `-CV` 오프셋 과선행 가드 적용 후 offset은 안정됐지만, 일부에서 cutoff가 따라 늘어나지 않아
    alias 길이가 짧아지는 문제 발생.
- 수정 파일
  - `Auto_OTO/core/oto_generator.py`
  - `Auto_OTO/core/ja_oto_generator.py`
- 변경 사항
  - KR/JA `cv_head` 오프셋 가드에서 파라미터 재계산 방식을 변경.
    - 기존: `pre/cons/cutoff` 절대시각 보존(=offset 이동 시 상대 길이 축소)
    - 변경: `pre/cons/cutoff` 상대 길이 우선 보존(길이 단축 방지)
  - KR `cv_head` 분기 처리 순서 보강.
    - post pipeline에서 cutoff guard를 먼저 적용하지 않고,
      `offset 과선행 가드` 이후 `cutoff next-onset guard`를 재적용하도록 조정.
    - offset 보정 후 cutoff 안전 상한을 다시 계산해 길이/안전성을 동시 확보.
- 기대 효과
  - 어두 `-CV`가 공백 과포함 없이 시작점은 안정화하면서도,
    이전 대비 과도하게 짧아진 alias 길이를 완화.
- 검증
  - `python -m py_compile Auto_OTO/core/oto_generator.py Auto_OTO/core/ja_oto_generator.py`
  - `python Auto_OTO/scripts/test_profile_timing_presets.py` (PASS)
  - `$env:PYTHONUTF8='1'; python Auto_OTO/test_auto.py` (PASS)

## 52) 2026-02-27 일본어 `-CV`(특히 `- ちょ`) 컷오프 과단축 재발 수정
- 증상
  - 오프셋은 안정화되었지만 `-CV` 일부에서 컷오프가 지나치게 짧아져 모음이 거의 포함되지 않음.
  - 실제 사례: `_ち_ちゅ_ちぇ_ちょ.wav=- ちょ`가 모음 진입 전에 닫히는 상태.
- 원인
  - 템플릿 base shape의 비정상 head 라인(`cut_gap` 과소, `pre` 과대)이 `cv_head` 블렌딩으로 다시 유입.
  - offset 가드 이후에도 짧은 컷오프 값이 유지되어 길이 복원이 충분히 되지 않음.
- 수정 파일
  - `Auto_OTO/core/ja_oto_generator.py`
  - `Auto_OTO/core/oto_generator.py`
- 수정 내용
  - `cv_head` base-shape 블렌딩 보호 규칙 추가.
    - `cut_gap < 90ms` 또는 `pre > 280ms`인 템플릿 shape는 `cv_head`에서 블렌딩 생략.
  - `cv_head` 최소 모음 포함 가드 보강.
    - 컷오프 하한 계산에 `pre + 최소꼬리길이` 조건을 추가해 자음만 남는 단축 방지.
    - 일본어는 `curr_phones`에서 직접 모음 phone을 다시 잡아(마지막 모음 기준) 커버리지 계산.
- 검증 결과(재생성)
  - 대상: `SODA_D4_CVVC_8mora_Ultimate+ 2025-08-25`
  - `- ちょ` 개선:
    - 수정 전: `offset=3421.92, cut_abs=3778.42` (모음 시작 전/직전 종료)
    - 수정 후: `offset=3460.00, cut_abs=4148.02` (모음 구간 충분히 포함)
- 검증
  - `python -m py_compile Auto_OTO/core/oto_generator.py Auto_OTO/core/ja_oto_generator.py`
  - `python Auto_OTO/scripts/test_profile_timing_presets.py` (PASS)

## 53) 2026-02-27 리팩토링(성능/유지보수) 진행
- 목적
  - OTO 생성/보정 루프에서 반복되는 alias 분류 호출을 캐시화해 처리 속도를 안정적으로 개선.
  - 불필요한 반복 순회 코드를 제거해 유지보수성을 높임.
- 변경 파일
  - `Auto_OTO/core/oto_generator.py`
  - `Auto_OTO/core/ja_oto_generator.py`
- 주요 변경
  - 한국어/일본어 생성기에서 `classify_alias`/`classify_ja_alias` 반복 호출 구간에 함수 단위 캐시(`dict`) 적용.
  - 프로파일 학습/적용, 멜 보정, 포맷 감지, 타겟 추출 루프에 동일 캐시 패턴 통일.
  - 일본어 생성기 내 사용되지 않던 alias 순회 블록 제거.
  - 한국어 `cv_head` 경로에서 `n_start/n_end` 미정의로 발생하던 런타임 오류 수정
    - `_prepare_cv_head_syllable_timing` 반환값에 `n_start`, `n_end` 추가
    - 호출부에서 해당 값을 사용하도록 연동
- 검증
  - `python -m py_compile core/oto_generator.py core/ja_oto_generator.py` 통과
  - `python scripts/test_profile_timing_presets.py` 통과
  - `PYTHONUTF8=1 python test_auto.py` 통과

## 54) 2026-03-01 OTO 수치 보정 AI 작업공간/런타임 1차 구축
- 작업 폴더 정리
  - 저장소 내부 추적 코드: `Auto_OTO/ml`
  - 상위 실험 작업공간: `../ml_workspace`
  - 배포/런타임 모델 경로: `assets/models/oto_ml/<language>/<format>/v1`
- 추가 파일
  - `requirements-ml.txt`
  - `core/oto_ml_features.py`
  - `core/oto_ml_dataset.py`
  - `core/oto_ml_runtime.py`
  - `core/oto_ml_lightgbm.py`
  - `core/oto_ml_refiner.py`
  - `core/oto_ml_pytorch.py`
  - `ml/scripts/build_oto_ml_dataset.py`
  - `ml/scripts/train_oto_ml_model.py`
  - `ml/scripts/evaluate_oto_ml_model.py`
  - `ml/tests/*.py`
- 구현 내용
  - alias/format 자동 분류를 재사용하는 OTO ML feature 추출기 추가
  - 자동 OTO + 수동 OTO + TextGrid + WAV를 이용한 학습 CSV 생성 CLI 추가
  - LightGBM 기반 delta 회귀 학습/평가 스크립트 추가
  - backend 추상화 런타임 추가(현재 LightGBM 구현, PyTorch stub 준비)
  - 한국어/일본어 생성기 후단에 ML 보정 계층 연동
    - 모델이 없거나 의존성이 없으면 자동 fallback
  - `.gitignore`에 ML 캐시/산출물 규칙 추가
- 현재 상태
  - 1차는 LightGBM 학습/추론 경로까지 구현
  - 실제 모델 파일은 아직 미학습 상태이며 `.gitkeep`만 배치
  - 생성기 연동은 완료됐고, 모델이 준비되면 자동 적용 가능

## 55) 2026-03-01 OTO 수치 보정 AI 1차 학습/추론 검증
- 실행 내용
  - `requirements-ml.txt` 기준으로 `lightgbm` 설치를 완료했다.
  - `Dabi_Test` 자료를 사용해 자동 OTO(`oto_AutoTest2.ini`)와 수동 OTO(`oto.ini`)를 비교하는 학습용 CSV를 생성했다.
  - 출력 CSV: `../ml_workspace/datasets/japanese/dataset_dabi_test.csv`
  - 저장 row 수: 335, 매칭 성공 row 수: 335, 스킵 row 수: 0
- 데이터셋 관찰
  - `Dabi_Test`는 일본어 자료이지만 감지된 `format_type`이 단일 형식이 아니라 `cvc`, `cvvc`, `mono`, `vc_only`로 혼합되어 있었다.
  - 분포상 `cvc` 비중이 가장 커서 형식 전용 모델과 공용 fallback 모델을 함께 두는 전략이 유효함을 확인했다.
- 학습/평가 결과
  - 공용 모델: `assets/models/oto_ml/japanese/general/v1`
  - 형식 전용 모델: `assets/models/oto_ml/japanese/cvc/v1`
  - `japanese/general` 평가 요약
    - `delta_offset` baseline MAE 33.78 -> model MAE 33.46
    - `delta_cons` baseline MAE 33.81 -> model MAE 30.14
    - `delta_cutoff` baseline MAE 44.38 -> model MAE 38.92
    - `delta_pre` baseline MAE 23.76 -> model MAE 23.43
    - `delta_ovl` baseline MAE 10.79 -> model MAE 9.97
  - `japanese/cvc` 평가 요약
    - `delta_offset` baseline MAE 25.98 -> model MAE 20.25
    - `delta_cons` baseline MAE 24.74 -> model MAE 14.96
    - `delta_cutoff` baseline MAE 36.20 -> model MAE 29.65
    - `delta_pre` baseline MAE 18.69 -> model MAE 12.62
    - `delta_ovl` baseline MAE 10.04 -> model MAE 6.14
- 추론 경로 검증
  - `ml_workspace/exports/dabi_ml_apply_test.ini` 복사본에 ML 후처리를 적용해 실제 추론 경로를 검증했다.
  - mixed format 파일에서 `cvc`는 `japanese/cvc/v1`, 기타 형식은 `japanese/general/v1`을 사용하는 fallback 흐름이 동작했다.
  - 테스트 복사본 기준 335행이 재계산되어 저장되었다.
- 추가 보강
  - `core/oto_ml_refiner.py`에 콜백 안전 출력 래퍼를 추가해 콘솔 인코딩 오류 때문에 추론이 중단되지 않도록 수정했다.
  - 동일한 `general` 모델 경로를 여러 형식이 공유할 때 로드 로그가 중복 출력되지 않도록 정리했다.
- 현재 상태
  - ML 런타임, 학습 스크립트, 평가 스크립트, 일본어 공용/`cvc` 1차 모델까지 실제 동작 확인 완료.
  - 다음 단계는 한국어 자료(`KR-CVC`, `KR-CVVC`)에도 동일하게 dataset/train/eval을 적용하고, 필요하면 형식별 배포 모델을 선별하는 것이다.

## 56) 2026-03-01 일본어 포맷 분류 규칙/학습 루트 보강
- 일본어 포맷 분류 규칙을 조정했다.
  - `VC` 또는 `VV` 연결이 있으면 `CVVC`
  - `VCV` alias가 있으면 `VCV`
  - 그 외는 `CV`
  - 일본어에서 기존 `CVC` 표기는 내부적으로 더 이상 독립 포맷으로 보지 않고 `CV` 또는 `CVVC`로 흡수한다.
- 반영 파일
  - `core/ja_oto_generator.py`
  - `core/oto_profile_presets.py`
  - `core/oto_ml_features.py`
- 학습용 alias 매칭을 보강했다.
  - alias 끝의 음계 접미사(`_C4`, `_D4`, `-A4`, ` F4` 등)를 제거한 뒤 `alias_norm`을 계산하도록 수정했다.
  - 다음계/다음 음역 하위폴더 자료처럼 같은 alias에 음계 접미사가 붙는 자료를 학습셋에 더 안정적으로 합칠 수 있게 됐다.
- ML 설정 파일 추가
  - `ml/configs/training_data_roots.yaml`
  - 일본어 VCV/CVVC, 한국어 CVVC/CVC 학습 루트를 정리했고, `波音リツEve`도 일본어 CVVC 루트에 추가했다.
- 보강 문서
  - `ml/README.md`를 UTF-8 한국어 기준으로 다시 정리했다.

## 57) 2026-03-01 일본어 `CVC` 모델 제거 및 `CV/CVVC` 재학습
- 일본어 포맷 규칙 변경 후 `Dabi_Test` 학습셋을 다시 생성했다.
- 새 분포
  - `cvvc`: 286 rows
  - `cv`: 49 rows
- 학습 모델 경로
  - `assets/models/oto_ml/japanese/general/v1`
  - `assets/models/oto_ml/japanese/cvvc/v1`
  - `assets/models/oto_ml/japanese/cv/v1`
- 기존 `japanese/cvc/v1`에 생성했던 LightGBM 산출물은 제거했다.
- 평가 요약
  - `japanese/cvvc`
    - `delta_offset` 31.08 -> 29.02
    - `delta_cons` 29.65 -> 24.33
    - `delta_cutoff` 40.71 -> 38.08
    - `delta_pre` 22.93 -> 22.40
    - `delta_ovl` 10.71 -> 8.64
  - `japanese/cv`
    - `delta_offset` 49.51 -> 47.13
    - `delta_cons` 58.11 -> 56.09
    - `delta_cutoff` 65.77 -> 65.39
    - `delta_pre` 28.60 -> 28.60
    - `delta_ovl` 11.23 -> 10.83
- 추론 검증
  - mixed format 복사본에 ML 후처리를 다시 적용해 `japanese/cvvc/v1`와 `japanese/cv/v1`가 실제로 분기 로드되는 것을 확인했다.

## 58) 2026-03-01 alias 매칭용 suffix 제거 규칙 일반화
- 기존 `strip_pitch_suffix_examples` 수준의 예시 제거만으로는 부족해, alias 코어를 유지하는 방식으로 suffix 제거 규칙을 일반화했다.
- 변경 내용
  - `core/oto_ml_features.py`
  - `_normalize_alias_for_match(alias, language, custom_map)`를 언어 인지형으로 확장했다.
  - 음계 접미사 제거 후에도 alias 끝에 붙은 음색/스타일 suffix를 반복적으로 제거한다.
  - 제거 조건은 `남은 prefix의 alias_type이 원본 alias_type과 같을 때만` 허용한다.
- 의도
  - `ga_soft -> ga`
  - `a か連 -> a か`
  - `lyeo부드러움 -> lyeo`
  - `a か` 같은 실제 연결 alias는 유지
- 효과
  - 단순 예시 목록이 아니라, 언어/형식 구조를 보존한 채 뒤에 붙은 비음운 꼬리 문자열을 학습 매칭에서 제거할 수 있게 됐다.

## 59) 2026-03-01 재귀 학습 데이터 수집 스크립트 추가
- 추가 파일
  - `core/oto_ml_collection.py`
  - `ml/scripts/collect_oto_ml_training_data.py`
  - `ml/tests/test_collection.py`
- 구현 내용
  - `training_data_roots.yaml`를 읽어 언어/형식별 루트를 재귀 탐색한다.
  - 각 루트에서 `manual oto`, `auto oto`, `TextGrid`, `WAV`를 자동 탐색한다.
  - 학습 가능한 항목은 형식별 CSV로 바로 빌드한다.
  - 학습 불가능한 항목은 manifest/CSV 리포트에 skip 사유를 기록한다.
  - 스크립트 재실행 시 기존 형식별 CSV를 덮어써 중복 append가 생기지 않도록 수정했다.
  - 설정 파일의 `notes` 블록은 루트 목록으로 잘못 해석하지 않도록 필터링했다.
- 실제 실행 결과
  - 총 후보: 12
  - 즉시 학습 가능: 1
  - 빌드 완료: 1
  - 스킵: 11
  - 생성 CSV: `../ml_workspace/datasets/japanese/dataset_japanese_cvvc.csv`
  - 생성 row 수: 335
- 스킵 사유 요약
  - `missing_auto_oto_textgrids`: 11
- 해석
  - 현재 추가해 둔 루트 대부분은 `수동 OTO + WAV` 참조 뱅크로는 유효하지만, ML 학습용으로 바로 쓰려면 자동 OTO 페어와 TextGrid가 더 필요하다.

## 60) 2026-03-01 상위 빈 `oto.ini` 자동 제외 규칙 추가
- 음색/음계별 하위 폴더가 있는 보이스뱅크에서 최상위 `oto.ini`가 0바이트 placeholder인 경우가 있어, 학습/배치 준비 탐색 규칙을 수정했다.
- 변경 내용
  - `core/oto_ml_collection.py`
  - `core/oto_ml_batch_prepare.py`
  - 실제 OTO 라인(`wav=alias,...`)이 없는 `.ini`는 manual/base OTO 후보에서 제외한다.
- 효과
  - 상위 빈 `oto.ini`는 무시하고, 하위 음계/음색 폴더의 실제 `oto.ini`만 작업 대상으로 잡는다.
- 검증
  - `ml/tests/test_staging_prepare.py` 추가
  - 빈 상위 `oto.ini` + 유효한 하위 `oto.ini` 구조에서 하위 파일만 선택되는지 확인 완료.

## 61) 2026-03-01 ML/배치 파이프라인 병목 계측
- 추가 파일
  - `ml/scripts/profile_oto_ml_pipeline.py`
- 목적
  - 현재 ML 학습/배치 파이프라인에서 실제 병목이 학습인지, 데이터셋 구축인지, MFA 정렬인지 수치로 확인하기 위한 프로파일링 스크립트를 추가했다.
- 계측 항목
  - 설정 파일 기준 학습 후보 탐색 시간
  - `dataset` 스테이징 폴더 기준 학습 후보 탐색 시간
  - staged work item 탐색 시간
  - staged 후보에서 학습 CSV를 다시 구축하는 시간
  - 아직 자동 OTO/TextGrid가 없는 항목 1건에 대한 실제 준비 시간
  - 선택한 CSV에 대한 LightGBM 학습/평가 시간
- 계측 결과 요약
  - 설정 파일 기준 후보 탐색: 약 `0.36초`
  - staged dataset 기준 후보 탐색: 약 `0.23초`
  - staged work item 탐색: 약 `0.04초`
  - staged 후보 23건에서 학습 CSV 재구축: 약 `434.35초`
  - 일본어 `cvvc` CSV(`1853` rows) 학습: 약 `0.30초`
  - 일본어 `cvvc` CSV 평가: 약 `0.14초`
- 해석
  - 현재 병목은 LightGBM 학습이 아니라 `TextGrid/oto/wav`를 읽어 feature row를 다시 만드는 데이터셋 구축 단계다.
  - 실제 미생성 1건 계측에서도 `prepare_lab_and_dict`는 거의 즉시 끝났고, 시간이 걸린 단계는 `MFA align`이었다.
  - 따라서 지금 구조에서는 GPU(CUDA)보다 `MFA 정렬`과 `dataset build` 최적화가 우선순위가 높다.
- 추가 확인 사항
  - 미생성 1건 테스트 중 `波音リツEve\\A3子音` 항목에서 MFA가 Windows 사용자 폴더 아래 경로에 `phones.txt`를 쓰는 단계에서 실패했다.
  - 비 ASCII 경로 또는 MFA 작업 디렉터리 경로 처리 문제일 가능성이 있어, 이후 MFA 실행 경로/작업 디렉터리 정리가 필요하다.

## 62) 2026-03-01 MFA 안전 경로 + OTO ML feature 캐시
- 추가/수정 파일
  - `core/mfa_runner.py`
  - `core/oto_ml_features.py`
  - `ml/tests/test_mfa_runner.py`
  - `ml/tests/test_feature_cache.py`
- MFA 안전 경로 처리
  - Windows에서 `wav_folder`, `dict_path`, `output_folder` 중 하나라도 비 ASCII 경로를 포함하면, MFA 실행 전에 ASCII-safe 임시 작업공간을 만든다.
  - 이 작업공간에는 `.wav`, `.lab`, 사전 파일을 ASCII 경로로 하드링크 또는 복사해 둔다.
  - MFA 정렬 성공 시 생성된 `TextGrid`를 원래 `output_folder`로 다시 복사한다.
  - 추가로 `MFA_ROOT_DIR`도 앱 내부 ASCII 경로로 고정해 MFA의 내부 임시 폴더가 사용자 문서 경로의 비 ASCII 이름에 영향을 받지 않도록 했다.
- 인코딩 보강
  - MFA subprocess 출력 디코딩은 고정 `utf-8` 대신 시스템 기본 인코딩을 사용하도록 바꿨다.
  - 일본어 모델 미설치나 Windows 콘솔 환경에서 발생하던 출력 디코드 예외를 줄이기 위한 조치다.
- OTO ML feature 디스크 캐시
  - `extract_feature_rows(...)` 결과를 `Auto_OTO/.cache/oto_ml_features` 아래 JSON 캐시로 저장하도록 추가했다.
  - 캐시 키는 `language`, `FEATURE_VERSION`, `oto/tg_dir/wav_dir/custom_phonemes`의 경로/수정시각/파일 수 정보를 바탕으로 계산한다.
  - 동일 입력으로 다시 dataset build를 돌릴 때 `TextGrid + wav + mel` feature 재계산을 생략한다.
- 재측정 결과
  - staged candidate 재수집/재구축 1회차: 약 `608.97초`
  - 동일 staged candidate 재수집/재구축 2회차(캐시 재사용): 약 `105.55초`
  - 캐시 파일 생성 수: `38`
  - 즉, 현재 dataset build 재실행 시간은 약 `6배` 가까이 단축됐다.
- 추가 확인 사항
  - 비 ASCII 일본어 하위 폴더 1건 테스트에서는 경로 문제 대신 `japanese_mfa` 모델 미설치가 새 병목으로 확인됐다.
  - 따라서 경로 우회는 적용됐고, 해당 항목을 실제 끝까지 검증하려면 일본어 MFA 모델 설치 상태를 먼저 맞춰야 한다.

## 63) 2026-03-01 OTO ML training row 캐시 추가
- 추가/수정 파일
  - `core/oto_ml_features.py`
  - `ml/tests/test_feature_cache.py`
- 변경 내용
  - `build_training_rows(...)` 결과 자체를 디스크 캐시하도록 추가했다.
  - 캐시 키는 `language`, `auto_oto`, `manual_oto`, `tg_dir`, `wav_dir`, `custom_phonemes`, `voicebank_id`, `FEATURE_VERSION` 기반으로 계산한다.
  - 저장 위치는 기본적으로 `Auto_OTO/.cache/oto_ml_training_rows`이다.
  - 이 캐시는 `feature cache` 이후에도 남아 있던 수동 OTO 파싱, occurrence 매칭, delta 계산 비용을 건너뛰기 위한 것이다.
- 테스트
  - `test_feature_cache.py`에 training row 캐시 우선 사용 테스트를 추가했다.
- 재측정 결과
  - row cache 삭제 후 staged dataset 재구축 1회차: 약 `71.24초`
  - 같은 staged dataset 재구축 2회차: 약 `3.77초`
  - 생성된 row cache 파일 수: `37`
- 해석
  - 현재는 `feature cache + training row cache`가 모두 채워진 뒤에는 dataset rebuild 시간이 수 초 단위로 줄어든다.
  - 따라서 학습용 CSV를 반복 생성하면서 실험하는 흐름에서는 병목이 거의 제거된 수준이다.
- 배포 메모
  - 현재 LightGBM 기준 배포 단위는 일반적으로 `checkpoint`보다는 `모델 번들(model bundle)` 표현이 더 정확하다.
  - 실제 배포 파일은 `model_*.txt`, `feature_schema.json`, `model_meta.json`, `eval_summary.json` 조합이다.

## 64) 2026-03-01 모델 번들 익스포트 스크립트 + 형식별 모델 학습
- 추가 파일
  - `core/oto_ml_export.py`
  - `ml/scripts/export_oto_ml_bundle.py`
  - `ml/tests/test_export_bundle.py`
- 익스포트 스크립트 기능
  - 배포에 필요한 LightGBM 번들 파일만 골라 익스포트한다.
  - 포함 파일
    - `feature_schema.json`
    - `model_meta.json`
    - `eval_summary.json`
    - `model_offset.txt`
    - `model_cons.txt`
    - `model_cutoff.txt`
    - `model_pre.txt`
    - `model_ovl.txt`
  - 번들별 `bundle_manifest.json` 생성
  - 전체 `export_manifest.json` 생성
  - 선택적으로 zip 아카이브 생성 가능
- 테스트
  - 번들 디렉터리 검증 테스트
  - zip 포함 익스포트 테스트
- 공용 dataset 생성
  - `ml_workspace/datasets/japanese/dataset_japanese_general.csv` (`21833 rows`)
  - `ml_workspace/datasets/korean/dataset_korean_general.csv` (`7617 rows`)
- 학습 완료 번들
  - 일본어
    - `assets/models/oto_ml/japanese/cvvc/v1`
    - `assets/models/oto_ml/japanese/vcv/v1`
    - `assets/models/oto_ml/japanese/general/v1`
  - 한국어
    - `assets/models/oto_ml/korean/cvc/v1`
    - `assets/models/oto_ml/korean/cvvc/v1`
    - `assets/models/oto_ml/korean/general/v1`
- 학습 요약
  - 일본어 `cvvc`: `1853 rows`, `voicebank_count=2`
  - 일본어 `vcv`: `17320 rows`, `voicebank_count=3`
  - 일본어 `general`: `21833 rows`, `voicebank_count=6`
  - 한국어 `cvc`: `378 rows`, `voicebank_count=1`
  - 한국어 `cvvc`: `146 rows`, `voicebank_count=2`
  - 한국어 `general`: `7617 rows`, `voicebank_count=5`
- 해석 메모
  - 일본어 `vcv`/`general`은 holdout 기준 개선 폭이 충분히 확인됐다.
  - 한국어 형식 전용 모델은 현재 dataset 내부 `format_type` 분포상 실제 사용 row 수가 적다.
  - 특히 `dataset_korean_cvvc.csv` 안에도 `vcv`, `cvc`, `mono`로 분류된 row가 많아, 현재 기준으로는 한국어 `general` 모델의 안정성이 더 높다.
- 익스포트 결과
  - 배포용 익스포트 폴더: `ml_workspace/exports/model_bundles`
  - 생성된 zip
    - `japanese_cvvc_v1.zip`
    - `japanese_vcv_v1.zip`
    - `japanese_general_v1.zip`
    - `korean_cvc_v1.zip`
    - `korean_cvvc_v1.zip`
    - `korean_general_v1.zip`
  - 전체 manifest: `ml_workspace/exports/model_bundles/export_manifest.json`

## 65) 2026-03-01 런타임 번들 설치 경로 + 형식 override 재학습
- 추가/수정 파일
  - `core/oto_ml_bundle_install.py`
  - `ml/scripts/install_oto_ml_bundle.py`
  - `ml/tests/test_bundle_install.py`
  - `core/oto_ml_refiner.py`
  - `core/oto_ml_dataset.py`
  - `core/oto_ml_features.py`
  - `core/oto_ml_collection.py`
  - `ml/scripts/build_oto_ml_dataset.py`
- 런타임 번들 경로 정리
  - 앱 런타임은 이제 `models_installed/oto_ml/<language>/<format>/v1`를 먼저 찾고, 없으면 기존 `assets/models/oto_ml/...`를 fallback으로 사용한다.
  - `install_oto_ml_bundle.py`로 exported bundle 디렉터리 또는 zip을 설치 경로에 바로 반영할 수 있다.
  - 실제 설치 확인 결과 `_resolve_model_dir(...)`는 `models_installed` 경로를 우선 반환했다.
- 학습용 format override
  - dataset build 시 `candidate.format_type`를 강제로 row의 `format_type`으로 사용하도록 수정했다.
  - 그 결과 `dataset_korean_cvc.csv`와 `dataset_korean_cvvc.csv` 내부의 형식 혼합이 제거됐다.
- 재수집 결과
  - staged dataset 기준 `total=66`, `ready=45`, `built=45`, `saved_rows=34828`
- 형식 분포 확인
  - `dataset_korean_cvc.csv`: `2013 rows`, 전부 `cvc`
  - `dataset_korean_cvvc.csv`: `5604 rows`, 전부 `cvvc`
  - `dataset_japanese_cvvc.csv`: `1902 rows`, 전부 `cvvc`
  - `dataset_japanese_vcv.csv`: `25309 rows`, 전부 `vcv`
- 재학습 완료 번들
  - 일본어
    - `cvvc`: `1902 rows`, `voicebank_count=2`
    - `vcv`: `25309 rows`, `voicebank_count=5`
    - `general`: `27211 rows`, `voicebank_count=7`
  - 한국어
    - `cvc`: `2013 rows`, `voicebank_count=2`
    - `cvvc`: `5604 rows`, `voicebank_count=3`
    - `general`: `7617 rows`, `voicebank_count=5`
- 지표 메모
  - 일본어 `vcv`/`general`은 개선 폭이 크다.
  - 한국어 `cvc`는 전용 모델로 row 수가 정상화되며 개선이 확인됐다.
  - 한국어 `cvvc`는 row 수는 정상화됐지만 `delta_cutoff` holdout 지표가 악화되어 추가 feature/타깃 조정이 필요하다.
- 재익스포트 및 설치
  - `ml_workspace/exports/model_bundles` 아래 6개 zip을 다시 생성했다.
  - 동일 6개 번들을 `Auto_OTO/models_installed/oto_ml/...`로 설치 완료했다.
## 66) 2026-03-01 OTO ML 번들 v2 재익스포트 및 설치본 동기화
- 관련 파일
  - `core/oto_ml_features.py`
  - `core/oto_ml_lightgbm.py`
  - `core/oto_ml_refiner.py`
  - `core/oto_ml_export.py`
  - `core/oto_ml_bundle_install.py`
  - `ml/scripts/export_oto_ml_bundle.py`
  - `ml/scripts/install_oto_ml_bundle.py`
- 핵심 변경
  - `FEATURE_VERSION`를 `v2`로 올리고, `delta_cutoff` 학습 타깃을 상대 cutoff 값 차이에서 `절대 cutoff 위치 차이` 기준으로 변경했다.
  - 추론 단계에서도 `delta_cutoff`를 절대 cutoff 위치 보정값으로 해석하도록 맞췄다.
  - LightGBM 학습/평가에서 언어별 delta clip 범위를 그대로 적용해 과대 타깃이 모델을 흔들지 않도록 정리했다.
  - 재학습된 번들을 다시 export 후 `models_installed/oto_ml/...`에 재설치해 런타임과 학습 산출물을 동기화했다.
- 설치 경로 동기화 결과
  - `models_installed/oto_ml/japanese/cvvc/v1`: `feature_version=v2`, `train_rows=1902`, `voicebank_count=2`
  - `models_installed/oto_ml/japanese/vcv/v1`: `feature_version=v2`, `train_rows=32030`, `voicebank_count=5`
  - `models_installed/oto_ml/japanese/general/v1`: `feature_version=v2`, `train_rows=27211`, `voicebank_count=7`
  - `models_installed/oto_ml/korean/cvc/v1`: `feature_version=v2`, `train_rows=2013`, `voicebank_count=2`
  - `models_installed/oto_ml/korean/cvvc/v1`: `feature_version=v2`, `train_rows=5604`, `voicebank_count=3`
  - `models_installed/oto_ml/korean/general/v1`: `feature_version=v2`, `train_rows=7617`, `voicebank_count=5`
- 재학습 후 주요 평가 요약
  - 일본어 `cvvc`
    - `delta_offset`: `119.47 -> 115.39`
    - `delta_cons`: `87.08 -> 87.44`
    - `delta_cutoff`: `144.53 -> 124.74`
    - `delta_pre`: `71.84 -> 71.97`
    - `delta_ovl`: `23.97 -> 24.17`
  - 일본어 `vcv`
    - `delta_offset`: `188.72 -> 88.77`
    - `delta_cons`: `132.19 -> 23.47`
    - `delta_cutoff`: `179.70 -> 72.26`
    - `delta_pre`: `108.85 -> 25.43`
    - `delta_ovl`: `25.40 -> 10.90`
  - 일본어 `general`
    - `delta_offset`: `136.27 -> 108.09`
    - `delta_cons`: `116.74 -> 39.80`
    - `delta_cutoff`: `197.82 -> 49.79`
    - `delta_pre`: `86.41 -> 38.78`
    - `delta_ovl`: `19.71 -> 15.21`
  - 한국어 `cvc`
    - `delta_offset`: `205.37 -> 52.03`
    - `delta_cons`: `94.56 -> 62.03`
    - `delta_cutoff`: `250.80 -> 446.71` (악화)
    - `delta_pre`: `90.17 -> 52.31`
    - `delta_ovl`: `26.94 -> 20.89`
  - 한국어 `cvvc`
    - `delta_offset`: `202.30 -> 121.97`
    - `delta_cons`: `102.04 -> 91.37`
    - `delta_cutoff`: `246.79 -> 134.78`
    - `delta_pre`: `73.18 -> 59.22`
    - `delta_ovl`: `32.92 -> 31.92`
  - 한국어 `general`
    - `delta_offset`: `205.37 -> 49.59`
    - `delta_cons`: `94.56 -> 60.09`
    - `delta_cutoff`: `259.33 -> 251.60`
    - `delta_pre`: `90.17 -> 47.98`
    - `delta_ovl`: `26.94 -> 18.01`
- 해석
  - `delta_cutoff` 의미를 절대 위치 기준으로 바꾸면서 한국어 `cvvc`와 일본어 `vcv/general`은 뚜렷하게 안정화됐다.
  - 한국어 `cvc`는 cutoff만 별도로 악화돼, 다음 단계에서는 alias_type별 cutoff 규칙 또는 전용 타깃 재설계가 필요하다.
  - 일본어 `cv` 전용 번들은 아직 `v1` 상태지만, 런타임 탐색 순서상 설치된 `japanese/general/v1`가 먼저 사용되므로 실제 추론은 `v2` 계열로 fallback된다.
- 배포 산출물
  - export 폴더: `ml_workspace/exports/model_bundles`
  - zip 파일: `japanese_cvvc_v1.zip`, `japanese_vcv_v1.zip`, `japanese_general_v1.zip`, `korean_cvc_v1.zip`, `korean_cvvc_v1.zip`, `korean_general_v1.zip`
  - 전체 manifest: `ml_workspace/exports/model_bundles/export_manifest.json`

## 67) 2026-03-01 일본어 `-CV` 컷오프 과단축 재수정
- 관련 파일
  - `core/ja_oto_generator.py`
- 문제
  - 개발현황 52번에서 다뤘던 일본어 어두 `-CV` alias의 컷오프 과단축 문제가 일부 케이스에서 다시 남아 있었다.
  - 특히 `- ちょ` 같은 alias에서 cutoff가 모음 진입 전이나 직후에 닫혀, 실제 발음 대신 공백 구간만 OTO에 남는 현상이 있었다.
- 원인 정리
  - `cv_head` 초기 cutoff 계산이 `consonant + vowel_len * 0.25` 중심이라, 자음/오프셋 위치에 따라 모음 시작 전에도 cutoff가 형성될 수 있었다.
  - 이후 `CV_HEAD 모음 길이 보정`이 한번 cutoff를 늘려도, 바로 뒤의 `다음 onset 보호 가드`가 다시 cutoff를 줄이며 최소 모음 길이 보장을 깨뜨릴 수 있었다.
- 수정 내용
  - `cv_head` 전용 최소 cutoff 절대위치를 계산하는 `_ja_cv_head_min_cutoff_abs(...)` 보조 함수를 추가했다.
  - `-CV` 초기 cutoff를 단순 자음 기준이 아니라 `모음 시작 상대위치 + 최소 보존 길이` 기준으로 계산하도록 변경했다.
  - `_ensure_ja_cv_head_min_vowel_coverage(...)`가 같은 보조 함수를 사용하도록 정리해 기준을 일원화했다.
  - `_guard_ja_cv_cutoff_to_next_onset(...)`에 `alias_type`, `vowel_start_ms`, `vowel_end_ms` 인자를 추가했다.
  - `cv_head`일 때는 다음 음절 onset 직전에서 지나치게 짧아지지 않도록 tail 허용 폭을 조금 두고, 가능한 범위 안에서 최소 모음 길이 하한을 유지하도록 변경했다.
- 기대 효과
  - 어두 `-CV` alias가 모음 진입 전에 닫히는 현상을 줄인다.
  - 모음 길이 보정과 다음 onset 가드가 서로 충돌하지 않고, 같은 기준으로 cutoff를 유지한다.
- 검증
  - `python -m py_compile Auto_OTO\\core\\ja_oto_generator.py`
  - `python -c "import core.ja_oto_generator as m; print('ja_oto_generator import ok')"`
- 남은 확인 사항
  - 실제 보이스뱅크에서 `- ちょ`, `- しょ`, `- も`, `- と`, `- そ` 같은 어두 `-CV` alias를 다시 생성해 스펙트로그램 기준으로 재확인 필요.

## 68) 2026-03-01 일본어 VCV/VC/VV 오버랩-선행발음 간격 보정
- 관련 파일
  - `core/ja_oto_generator.py`
  - `ml/tests/test_feature_extraction.py`
- 문제
  - 일본어 VCV 계열에서 `VV`, `VC`, `VCV` alias의 overlap이 전반적으로 너무 앞쪽에 배치되어, `pre - ovl` 간격이 과도하게 커지는 문제가 있었다.
  - 특히 같은 노트에서도 올바른 원음설정보다 박자가 밀린 것처럼 들리는 원인이 되었다.
- 수정 내용
  - 브리지 계열용 `pre-ovl` 목표 간격을 계산하는 `_ja_bridge_overlap_window(...)`를 추가했다.
  - 최종 overlap을 이 목표 구간에 강제하는 `_clamp_ja_bridge_overlap(...)`를 추가했다.
  - `VC/VV/VCV`에 대해 `_adaptive_ja_overlap(...)`가 ratio 중심이 아니라 gap 중심으로 overlap을 계산하도록 변경했다.
  - `_post_adjust_params(...)` 마지막 단계에서 style profile/autotune 적용 이후에도 `VC/VV/VCV` overlap을 다시 한 번 gap 윈도우에 맞추도록 보강했다.
- 조정 방향
  - `VV`는 overlap이 가장 뒤에 오도록(가장 작은 gap)
  - `VC`의 치찰/파열음은 그보다 약간 앞이지만 기존보다 훨씬 뒤로
  - `VC`의 유성/비음/유음 등은 치찰/파열음보다도 조금 더 뒤로 오도록 설정했다.
- 검증
  - `python -m py_compile Auto_OTO\\core\\ja_oto_generator.py Auto_OTO\\ml\\tests\\test_feature_extraction.py`
  - `python -m unittest discover -s Auto_OTO\\ml\\tests -p "test_*.py"`

## 69) 2026-03-01 선행발음 중심 OTO 후처리 정렬 도입
- 관련 파일
  - `core/oto_generator.py`
  - `core/ja_oto_generator.py`
  - `ml/tests/test_feature_extraction.py`
- 배경
  - 사용자가 체감하는 박자감과 발성 위치는 `offset` 자체보다 `preutterance`를 기준으로 `overlap`, `consonant`, `cutoff`가 어디에 놓이느냐에 크게 좌우된다.
  - 기존 계산은 alias 타입별 절대 위치/비율 중심이 강해서, `pre`는 맞더라도 `ovl`, `cons`, `cutoff`가 `pre` 기준으로 너무 멀어지는 경우가 남아 있었다.
- 수정 내용
  - 한국어 생성기(`core/oto_generator.py`)에 `_kr_precenter_gap_targets(...)`, `_recenter_kr_params_around_pre(...)`를 추가했다.
  - 일본어 생성기(`core/ja_oto_generator.py`)에 `_ja_precenter_gap_targets(...)`, `_recenter_ja_params_around_pre(...)`를 추가했다.
  - 한국어는 `_apply_post_timing_pipeline(...)` 내부에서 soft mel/base shape/stabilize 이후, cutoff hard guard 직전에 `pre` 중심 gap 재정렬을 적용한다.
  - 일본어는 `_post_adjust_params(...)` 최종 반환 직전에 `pre` 중심 gap 재정렬을 적용한다.
- 적용 원리
  - `ovl_gap = pre - ovl`
  - `cons_gap = consonant - pre`
  - `cut_gap = abs(cutoff) - consonant`
  - 위 세 gap을 alias 타입(`cv`, `cv_head`, `vc`, `vv`, `vcv`)과 자음군 특성에 맞는 허용 구간으로 넣고, 목표값 쪽으로 약하게 블렌딩한 뒤 다시 `cons`, `cutoff`, `ovl`를 재구성한다.
- 기대 효과
  - 선행발음을 기준으로 나머지 파라미터가 정렬되어, 같은 노트에서도 박자가 밀리거나 당겨져 들리는 문제를 줄인다.
  - 한국어/일본어 모두에서 계산 구조를 `pre` 중심으로 통일해 이후 수동 보정/ML 보정의 기준점도 더 일관되게 만든다.
- 검증
  - `python -m py_compile Auto_OTO\\core\\oto_generator.py Auto_OTO\\core\\ja_oto_generator.py Auto_OTO\\ml\\tests\\test_feature_extraction.py`
  - `python -m unittest discover -s Auto_OTO\\ml\\tests -p "test_*.py"`


