# Deprecated CoarseCRNN Alignment

This folder contains the old frame-level CoarseCRNN TextGrid alignment stack.

Status: deprecated. Runtime alignment now uses MFA/sequence alignment, and OTO
generation uses the boundary scorer plus wav decoder path. Keep this code only
for reproducing old checkpoints or historical A/B analysis.
