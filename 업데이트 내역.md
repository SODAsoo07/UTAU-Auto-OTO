# 업데이트 내역

이 문서는 큰 업데이트 사항이 발생할 때마다 수정 내역을 누적 기록하기 위한 문서입니다.

## 작성 규칙
- 기능 추가, 구조 변경, 데이터/모델 포맷 변경, 런타임 동작 변경, 테스트 정책 변경처럼 영향 범위가 큰 수정만 기록합니다.
- 항목마다 날짜, 변경 목적, 핵심 변경점, 영향 파일/모듈, 검증 방법을 간단히 남깁니다.
- 사소한 오타 수정이나 영향 범위가 매우 작은 수정은 필요할 때만 기록합니다.

## 기록 템플릿
### YYYY-MM-DD - 변경 제목
- 목적:
- 핵심 변경:
- 영향 범위:
- 검증:
- 비고:

## 업데이트 이력
### 2026-03-11 - 전처리 준비 작업 재개/재시도 지원 추가
- 목적: MFA 정렬/auto oto 준비 작업이 중간에 끊기거나 일부 보이스뱅크에서 실패했을 때 전체를 처음부터 다시 돌리지 않도록 개선.
- 핵심 변경:
  - `prepared_auto_pairs.json`를 work item 단위 체크포인트로 사용하도록 변경.
  - 각 항목 처리 직후 리포트를 즉시 저장해 중단 후에도 진행 상황이 남도록 수정.
  - `prepare_pairs.py`에 `--resume`, `--retry-failed` 옵션 추가.
  - PowerShell 래퍼에 `-ResumePrepare`, `-RetryFailedPrepare` 옵션 추가.
- 영향 범위:
  - `core/oto_ml_batch_prepare.py`
  - `ml/scripts/coupled/prepare_pairs.py`
  - `ml/scripts/run_coupled_mel_oto_training.ps1`
  - `ml/tests/test_staging_prepare.py`
  - `plan/coupled_mel_oto_training_commands.md`
- 검증:
  - `python -m pytest ml/tests/test_staging_prepare.py -q`
- 비고:
  - `-StageSources`를 다시 실행하면 dataset 재구성이 발생할 수 있으므로, 준비 재개는 보통 스테이징을 다시 하지 않는 경우에 사용하는 편이 안전함.
