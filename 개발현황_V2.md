# Auto_OTO 개발현황 V2

## 1. 문서 목적
이 문서는 `Auto_OTO`의 현재 구현 상태와 핵심 기능만 빠르게 파악하기 위한 요약 문서다.
기존 `개발현황_정리.md`는 상세 변경 이력 보존용으로 유지하고, 이 문서는 현재 기준 스냅샷으로 관리한다.

## 2. 프로젝트 개요
`Auto_OTO`는 UTAU / OpenUtau용 `oto.ini`를 자동 생성하는 데스크톱 도구다.
기본 파이프라인은 다음 순서로 동작한다.

1. WAV 기반 Lab 생성
2. 사전(Dictionary) 생성
3. 음성 정렬(MFA 또는 SOFA)로 TextGrid 생성
4. TextGrid와 base oto, 파일명, 내부 규칙을 바탕으로 OTO 계산
5. 후처리 보정, 자동 검증, 선택적 ML 수치 보정 적용

## 3. 현재 디렉터리 구조
- `main.py`
  - 앱 진입점
- `ui/`
  - GUI 레이아웃, 탭 구성, 파이프라인 액션, 정렬/OTO 액션 믹스인
- `core/`
  - Lab 생성, MFA/SOFA 실행, 한국어/일본어 OTO 생성, 검증, ML 런타임
- `ml/`
  - ML 데이터셋 생성, 학습, 평가, 번들 export/install 스크립트 및 테스트
- `assets/models/oto_ml/`
  - 학습된 OTO ML 모델 번들 저장 위치
- `models_installed/oto_ml/`
  - 앱 런타임이 우선 참조하는 설치형 모델 번들 위치
- `dataset/`
  - 학습용 소스 스테이징 폴더
- `../ml_workspace/`
  - 비추적 실험 데이터셋, 리포트, export 산출물 작업 공간

## 4. 지원 언어 및 형식
### 한국어
- `CVC`
- `CVVC`
- `CV`, `CV_HEAD(-CV)`, `VC`, `VV`, `VCV`, `mono`, `br` 내부 처리 지원

### 일본어
- `CVVC`
- `VCV`
- `CV`, `CV_HEAD(-CV)`, `VC`, `VV`, `VCV`, `mono`, `br` 내부 처리 지원
- 일본어 자동 포맷 판정 규칙
  - `VC` 또는 `VV` 연결이 있으면 `CVVC`
  - `VCV` alias가 있으면 `VCV`
  - 그 외는 `CV`

## 5. GUI 및 사용자 워크플로우
- 단계별 실행 버튼 제공
  - Lab 생성
  - Dictionary 생성
  - 음성 정렬
  - OTO 생성
- 일괄 실행 파이프라인 제공
- 로그 창, 상태 표시, 오류 리포트 export 지원
- UI 로그는 진행 상황, 오류, 검증 요약만 표시
- 상세 로그는 `logs/log_*.txt`, 구조화 이벤트 로그는 `logs/events_*.jsonl`에 저장
- 창 크기가 작아도 하단 UI 접근 가능하도록 스크롤 지원
- 고급 옵션 영역 접기/펼치기 지원
- OTO 생성 단계에서 `ML 수치 보정 적용` 토글 지원
  - 끄면 규칙 기반 + 기존 후처리만 사용
  - 켜면 설치된 OTO ML 번들을 후단 보정에 사용
  - 현재 일본어 `VCV`는 ML 적용 범위를 `VC/VV/VCV` 브리지 계열로 제한하고, `CV/-CV`는 규칙 기반 유지
- 설정은 `config.json`에 자동 저장/불러오기

## 6. 정렬 엔진 지원
### MFA
- GUI 내 자동 설치 지원
- 한국어 의존성(`eunjeon`, `jamo`) 자동 준비 시도
- 일본어 토크나이저 의존성 자동 설치 시도
- `--single-speaker` 계열 옵션 자동 탐지 후 사용
- 비 ASCII 경로에서는 ASCII-safe 임시 작업공간으로 우회 실행
- 권한 문제, MSVC 필요 여부 등을 사용자 팝업으로 안내

### SOFA
- 별도 전용 가상환경(`.env_sofa`) 사용
- GUI 내 자동 설치 지원
- 모델 자동 다운로드 지원
- FFmpeg 로컬 준비 지원
- MFA 실패 시 SOFA 재시도 안내 제공

## 7. OTO 생성 핵심 기능
### 공통
- alias 자동 분류
  - `cv`, `cv_head`, `vc`, `vv`, `vcv`, `mono`, `br`
- `validate_oto_params`를 통한 기본 제약 보정
- 멜 에너지 기반 soft offset / cutoff guard
- base oto shape 블렌딩
- phone activity 안정화
- 자동 검증 리포트(`*.validation.txt`) 생성
- 자동 생성되지 못한 행을 로그 끝에 사유별로 기록

### 한국어 OTO 생성기
- `core/oto_generator.py`
- CVC/CVVC 기준 음절/음소 경계 계산
- 이중모음, 된소리, 유성/무성 자음군별 타이밍 조정
- 받침 계열(`N/L/M/NG/K/T/P/H`) 대응
- `VC`/받침형 `CVC` 기본 계산 보정
  - 인접 CV anchor 기반 `VC` pre를 실제 전이 길이 중심으로 더 짧고 보수적으로 계산
  - `VC` overlap을 pre 근처로 더 당겨 블렌딩 어색함 완화
  - `VC/VV` base shape 블렌딩 가중치 축소
- `pre` 중심 후처리 정렬 적용
  - `ovl_gap = pre - ovl`
  - `cons_gap = consonant - pre`
  - `cut_gap = abs(cutoff) - consonant`
  - 위 gap들을 alias 타입/자음군 특성 기준으로 재정렬

### 일본어 OTO 생성기
- `core/ja_oto_generator.py`
- CVVC/VCV 기준 음절 매핑, bridge 계산, alias 표기 변환
- 일본어 alias 출력 스타일 선택 지원
  - 원본 그대로
  - 히라가나
  - 로마자
- 파일명 조합 음절 파서 보강
  - `じぇ`, `しぇ`, `ちぇ` 같은 요음/확장음
  - `すぃ`, `ずぃ` 같은 소형모음 결합음을 2글자 1모라로 유지
- 반복 음절 매핑 tie-break 보정
  - 같은 조합 음절이 연속될 때 이전 음절로 되감기지 않도록, 동점이면 기대 인덱스 이후 후보를 우선
- `V-CV`, `VC`, `VV`, `VCV` 브리지 계산 강화

## 13. 최근 큰 업데이트
- 한국어 생성기 리팩토링 1차
  - `core/kr_oto_rules.py` 신설
  - 한국어 static rule / alias 분류 / 토큰화 / CV 매핑 규칙을 `core/oto_generator.py`에서 분리
  - `oto_generator.py`는 timing 계산과 후처리에 더 집중하도록 정리
  - 관련 회귀 테스트 `88`개 통과
- `-CV` 컷오프 과단축 방지 로직 적용
- 일본어 생성기는 내부 계산용 cutoff와 최종 OTO 파일 cutoff를 분리
  - 생성/ML/멜 보정 중에는 내부 상대 cutoff를 유지
  - 모든 후처리 완료 후 마지막에만 실제 OTO cutoff 값으로 변환
  - 뒤쪽 `VCV` alias에서 발생하던 OpenUtau `CutOffBeforeOffsetError` 방지
- 최종 cutoff 변환 후에도 OpenUtau 렌더 안정화를 위해
  - `active length`, `consonant`, `pre`, `ovl`를 OTO 파일 의미론 기준으로 다시 clamp
  - `IndexOutOfRangeException` 계열 렌더 오류를 줄이기 위한 안전장치 추가
- `pre` 중심 후처리 정렬 적용
- `VC/VV/VCV`는 `pre-ovl` 간격 기준으로 overlap을 다시 정렬

## 8. OpenUtau 관련 기능
- base oto가 없어도 OpenUtau 호환 alias 자동 생성 가능
- 일본어 alias 출력 시 앞 V가 있는 `VC`, `V-CV` 계열은 앞 V를 로마자로 유지
- `베이스 oto 없음` 옵션 제공

## 9. 프로파일 및 자동 튜닝
- 보이스 타입별 OTO 프로파일 프리셋 지원
- 한국어/일본어 형식별 profile preset 적용 가능
- 사용자가 일부 수동 설정한 OTO를 기반으로 profile 추출 가능
- 추출된 profile을 다시 자동 생성 OTO에 적용 가능
- base oto / reference oto shape를 이용한 보정 지원

## 10. ML 수치 보정 계층
### 목적
기존 규칙 기반 OTO를 대체하지 않고, 생성된 수치를 alias 단위로 미세 보정한다.

### 현재 구조
- feature 기반 LightGBM 회귀 번들 사용
- 보정 타깃
  - `delta_offset`
  - `delta_cons`
  - `delta_cutoff`
  - `delta_pre`
  - `delta_ovl`
- 적용 방식
  - 기본 OTO 생성 후 ML delta 적용
  - 이후 기존 validate / guard 유지
- 런타임 탐색 순서
  1. `models_installed/oto_ml/...`
  2. `assets/models/oto_ml/...`
  3. 없으면 ML 미적용

### 현재 번들 상태
- 일본어
  - `cvvc/v1`
  - `vcv/v1`
  - `general/v1`
- 한국어
  - `cvc/v1`
  - `cvvc/v1`
  - `general/v1`
- 현재 설치된 런타임 번들은 모두 `feature_version = v2`
- 한국어 `VCV` 런타임 라우팅 보강
  - `VV` alias는 `korean/cvvc` 모델 우선 사용
  - 종성 성격의 `VC` alias(`stop`, `nasal`, `liquid`)는 `korean/cvc` 모델 우선 사용
  - 나머지 `VCV` 행은 `korean/general` fallback 사용
- 한국어 `CVC` 런타임 라우팅 제한
  - `VC` 중 실제 종성 성격(`stop`, `nasal`, `liquid`)인 행에만 `korean/cvc` ML 보정 적용
  - `CV`, `CV_HEAD(-CV)` 등은 현재 ML 적용을 기본 비활성화
  - 이유: `SODAsoo KR CVC` 실사용 테스트에서 CV 계열이 과보정되어 전체 품질이 크게 무너지는 사례 확인
- 현재 체감 품질 평가
  - 일본어 ML 보정은 실사용 품질 향상 효과가 큼
  - 일본어 `VCV`는 cutoff 좌표계/후처리 버그 수정 후 재검증한 결과, `ML ON`이 `ML OFF`보다 체감 품질이 더 좋았음
  - 특히 일본어 `VV` 연결에서 ML 보정 효과가 크게 확인되었고, 현재는 ML 자체가 문제 원인이 아니라는 결론
  - 현재 체감 기준으로는 일본어 `VV`, `VC`에서 ML 이득이 가장 크고, `VCV`는 기본 규칙이 헷갈리는 범위를 조금 더 정확하게 좁혀주는 보조 역할에 가깝다고 판단됨
  - 이 판단을 반영해 일본어 `VCV` ML 라우팅은 `VV/VC/VCV`에 더해 `-CV(cv_head)`도 다시 `vcv` 모델 적용 대상으로 재개방함. 일반 `CV`는 계속 규칙 기반 우선 유지
  - 일본어 `ん(n)+유성음/비음` 브리지에서 선행발음/오프셋이 앞뒤로 흔들리는 문제를 줄이기 위해, `n-bridge`를 별도 감지해 boundary를 현재 자음 onset 쪽으로 더 강하게 붙이고 offset pad를 줄이는 보정이 추가됨
  - 한국어 ML 보정은 아직 안정적이지 않음
  - 특히 `한국어 CVC`는 ML 적용 시 규칙 기반보다 품질이 크게 악화된 사례가 확인되었으므로, 현재는 규칙 기반을 기본으로 두고 ML은 실험적 옵션으로 취급하는 편이 안전함
  - 일본어 `VCV`는 ML 적용 대상 alias를 브리지 계열로 제한했고, 생성 시 선택한 포맷을 ML 라우팅에 고정해 `VCV 생성 중 CVVC 모델이 함께 로드되는 문제`를 차단함
  - 일본어 ML 보정은 alias별 보수 정책을 추가함
    - `VCV`의 `n+유성음/비음` 브리지는 ML이 음절 위치를 과하게 흔드는 경향이 있어 `offset/pre` 중심 delta를 크게 약화
    - `CVVC`의 유성음/치찰음/파열음 계열은 ML이 `pre/cutoff`를 권장 지점보다 앞당기는 경향이 있어, 앞으로 당기는 방향 delta를 강하게 축소하고 `CV/-CV`의 offset delta는 사실상 동결
  - 한국어 `CVVC`는 현재 ML이 `CV/-CV` timing을 쉽게 무너뜨리는 상태라, ML 적용 대상을 `VV`와 종성 성격의 `VC(stop/nasal/liquid)`로 제한함. 일반 `CV/-CV`는 규칙 기반 우선 유지
  - 한국어 `.lab` 생성/사전 생성은 이제 파일명과 `.lab` 내용을 같은 토큰 규칙으로 해석함. `'` 같은 특수문자가 더 이상 음소로 오인되지 않으며, `_yu'ye'yu'yo'yu'yeo'yu'i'yu.wav` 같은 파일도 `yu / ye / yu / yo / yu / yeo / yu / i / yu`로 정상 분해됨
  - 한국어 `CVVC`는 TextGrid `words`보다 alias/filename 순서가 더 안정적인 경우가 많아, 음절 매핑 선택 로직을 조정해 `phones 핵 + alias target` 기반 음절열을 훨씬 우선 채택하도록 변경함
  - 한국어 OTO 생성 시 사용자가 형식을 `CVVC`로 강제하면 ML 보정도 동일한 `cvvc` 형식으로 고정 라우팅된다. 기존처럼 row별 오판정으로 `cvc/cvvc/general` 여러 모델이 동시에 섞여 로드되지 않음
  - 일본어 `VCV`/`CVVC` 최종 저장 단계의 cutoff 좌표계 변환 버그를 수정함. 기존에는 내부 active length cutoff를 파일 cutoff 의미론으로 먼저 잘못 sanitize해서, ML 미적용본도 cutoff가 과대 음수로 붕괴했음. 현재는 내부 좌표계용 sanitize와 최종 OTO cutoff 변환을 분리해 정상 범위로 복구됨
  - 일본어 공통 후처리 `_post_adjust_params(...)`가 alias별 종점이 아니라 파일 전체 `effective_end_ms`를 기준으로 cutoff를 다시 묶어, 같은 wav 안 alias들의 cutoff가 거의 파일 끝으로 몰리는 구조를 수정함. 현재는 `VCV/CV/CV_HEAD/VC/VV/br`가 각자 local end(`n_end`, `v_end`, `br_end`) 기준으로 cutoff 상한을 잡음

### ML 작업 도구
- 데이터셋 빌드: `ml/scripts/build_oto_ml_dataset.py`
- 다중 루트 수집: `ml/scripts/collect_oto_ml_training_data.py`
- 학습: `ml/scripts/train_oto_ml_model.py`
- 평가: `ml/scripts/evaluate_oto_ml_model.py`
- export: `ml/scripts/export_oto_ml_bundle.py`
- install: `ml/scripts/install_oto_ml_bundle.py`

### 캐시
- feature cache: `Auto_OTO/.cache/oto_ml_features`
- training row cache: `Auto_OTO/.cache/oto_ml_training_rows`
- 반복 데이터셋 재구축 속도는 캐시 적용 후 크게 단축됨

## 11. 학습 데이터 관리
- 저장소 내부 코드/스키마/스크립트는 `Auto_OTO/ml`
- 비추적 실험 산출물은 `../ml_workspace`
- 스테이징된 학습 소스는 `Auto_OTO/dataset`
- 상위 빈 `oto.ini`는 학습용 manual oto 후보에서 제외
- alias 정규화 시 음계 suffix뿐 아니라 뒤에 붙은 스타일/음색 접미사도 제거하도록 보강됨

## 12. 설치 및 배포
- 앱 빌드 스크립트: `build.py`
- 배포 산출물은 `dist/` 기준
- FFmpeg는 SOFA용 로컬 준비 경로를 지원
- 일반 사용자용으로는 exe + 필요한 런타임/모델 번들 조합을 배포하는 구조를 지향
- ML 모델은 checkpoint 개념보다 `번들` 개념으로 관리
  - `model_*.txt`
  - `feature_schema.json`
  - `model_meta.json`
  - `eval_summary.json`
  - 선택적 `bundle_manifest.json`

## 13. 현재 테스트 체계
- 단위 테스트: `Auto_OTO/ml/tests/`
- 프로파일/타이밍 스모크 테스트: `scripts/test_profile_timing_presets.py`
- 기본 자동 테스트: `test_auto.py`
- 자주 쓰는 검증 명령
  - `python -m unittest discover -s Auto_OTO\ml\tests -p "test_*.py"`
  - `python scripts/test_profile_timing_presets.py`

## 14. 최신 학습 반영 현황
- `ml/configs/training_data_roots.yaml` 기준으로 등록된 학습 루트 `16개`는 현재 모두 `Auto_OTO/dataset` 아래로 스테이징 완료
- 스테이징 후 `prepare_staged_auto_pairs.py` 기준
  - 총 work item: `71`
  - 준비 완료: `69`
  - 스킵: `2`
- 스킵된 2건은 특수 폴더
  - `波音リツEve/無声子音`
  - `PiER -Allegro-/breath`
  - 둘 다 `auto oto + TextGrid`가 준비되지 않아 학습셋에 제외됨
- `collect_oto_ml_training_data.py` 기준
  - built: `69`
  - saved_rows 총합: `48179`
- 최신 데이터셋 크기
  - 한국어 `cvc`: `2637`
  - 한국어 `cvvc`: `11604`
  - 한국어 `general`: `14241`
  - 일본어 `cvvc`: `1908`
  - 일본어 `vcv`: `32030`
  - 일본어 `general`: `33938`

## 15. 최신 재학습(profile_run_v4) 요약
- 새 후보 번들 생성
  - `korean/cvc/profile_run_v4`
  - `korean/cvvc/profile_run_v4`
  - `korean/general/profile_run_v4`
  - `japanese/cvvc/profile_run_v4`
  - `japanese/vcv/profile_run_v4`
  - `japanese/general/profile_run_v4`
- 한국어 후보 판단
  - `korean/cvc/profile_run_v4`
    - train_rows `95`, voicebank `2`
    - cutoff 외 주요 지표가 악화되어 설치 보류
  - `korean/cvvc/profile_run_v4`
    - train_rows `1729`, voicebank `6`
    - `offset/pre/ovl`은 개선, `cutoff`는 악화
    - 현재 설치본 `korean/cvvc/v1(feature v2)`가 더 안정적이라 교체 보류
  - `korean/general/profile_run_v4`
    - train_rows `1584`, voicebank `9`
    - `cutoff/pre`가 악화되어 설치 보류
- 일본어 후보 판단
  - `japanese/vcv/profile_run_v4`
    - train_rows `32030`, voicebank `5`
    - 주요 타깃 전반 개선 폭이 큼
  - `japanese/cvvc/profile_run_v4`
    - train_rows `1908`, voicebank `2`
    - 완만한 개선
  - `japanese/general/profile_run_v4`
    - train_rows `33938`, voicebank `7`
    - mixed 결과지만 활용 가능성 있음
- 현재 결론
  - `training_data_roots.yaml`에 추가한 데이터는 스테이징과 학습 데이터셋 재구축까지 반영됨
  - 다만 설치형 런타임 번들은 품질이 더 나아진 후보만 선별 교체해야 하므로, 이번 `v4`는 전부 자동 교체하지 않고 보수적으로 유지함
  - 현재 런타임 설치본 유지 결정
    - 일본어 `cvvc/v1`, `vcv/v1`, `general/v1` 유지
    - 한국어 `cvc/v1`, `cvvc/v1`, `general/v1` 유지
  - 이유
    - 일본어 `profile_run_v4`는 데이터 규모는 늘었지만, 현재 설치된 `v1(feature v2)` 대비 명확한 우위가 확인되지 않았음
    - 한국어 `profile_run_v4`는 `cvvc`가 `offset/pre/ovl`만 일부 개선되고 `cutoff`가 악화됨
    - 한국어 `cvc`, `general`도 주요 지표가 불안정해 교체 가치가 부족함
  - 따라서 테스트는 현재 설치된 런타임 번들 기준으로 진행하는 것이 안전함

## 16. PyTorch 브리지 보정 파이프라인
- PyTorch 1차 적용 범위는 브리지 전용으로 제한
  - 일본어 `VCV`: `vc`, `vv`, `vcv`, `cv_head`
  - 일본어 `CVVC`: `vc`, `vv`, `cv_head`
  - 한국어 `CVVC`: `vc`, `vv`
  - 한국어 `CVC`: `vc`
  - 한국어 `CV/CV_HEAD`는 여전히 baseline/규칙 기반 우선
- 새 core 모듈 추가
  - `core/oto_torch_features.py`
  - `core/oto_torch_dataset.py`
  - `core/oto_torch_model.py`
  - `core/oto_torch_trainer.py`
  - `core/oto_torch_export.py`
  - `core/oto_ml_pytorch.py` 구현 완료
- 새 CLI 추가
  - `ml/scripts/build_oto_torch_dataset.py`
  - `ml/scripts/train_oto_torch_model.py`
  - `ml/scripts/evaluate_oto_torch_model.py`
  - `ml/scripts/export_oto_torch_bundle.py`
- 새 테스트 추가
  - `ml/tests/test_torch_dataset.py`
  - `ml/tests/test_torch_bundle.py`
  - `ml/tests/test_torch_runtime.py`
- 기존 ML feature CSV도 PyTorch에서 재사용 가능하도록 확장
  - `feature_version = v4`
  - 추가 feature
    - `mapping_confidence`
    - `used_alias_occurrence_mapping`
    - `used_exact_vowel_fix`
    - `used_nuclei_fallback`
    - `used_alias_based_syllables`
    - `words_vs_alias_score_margin`
    - `local_peak_db`
    - `local_valley_db`
    - `mel_window_energy_mean`
    - `mel_window_silence_ratio`
- PyTorch dataset 구조
  - 입력 CSV는 기존 `collect_oto_ml_training_data.py` 산출물을 그대로 사용
  - 추가로 `Auto_OTO/.cache/oto_torch_windows`에 per-wav log-mel cache 생성
  - voicebank 단위 shard `.npz` 생성
  - `index.json`에 shard/feature/summary 메타데이터 저장
- 모델 구조
  - `small CNN + tabular fusion regressor`
  - 입력
    - fixed-size log-mel window (`80 x 160`)
    - numeric feature vector
    - categorical embedding
    - `voicebank_id` embedding
  - 출력
    - `delta_offset`
    - `delta_cons`
    - `delta_cutoff`
    - `delta_pre`
    - `delta_ovl`
- 학습 손실
  - Huber loss
  - 구조 제약 penalty(`cons>=pre`, `pre>=ovl`, `active_length>=cons` 등)
  - `pre-ovl gap` 안정성 penalty
- 런타임
  - `core/oto_ml_runtime.py`에서 `backend = pytorch` bundle을 로드 가능
  - `predict_oto_deltas()`도 동작
  - 단, 현재 앱 기본 자동 적용은 여전히 OFF로 두고 오프라인 평가 우선
- 의존성
  - `requirements-ml-torch.txt` 추가
  - PyTorch는 CUDA 빌드가 이미 설치된 환경에서 바로 학습 가능
- 현재 하드웨어 판단
  - `RTX 3060 Ti 8GB`에서 초기 브리지 전용 학습은 가능
  - 실제 환경에서 CUDA 인식 확인됨
  - 대여 GPU는 larger-context/Transformer/대규모 튜닝 단계 전까지는 필수 아님
- 현재 검증 상태
  - synthetic dataset 기준 `dataset build -> train -> export -> runtime load/predict` 테스트 통과
  - 실제 `dataset_japanese_cvvc.csv` 기준 bridge dataset build smoke 통과
  - 실제 전체 epoch 학습은 작업 환경 타임아웃 제약 때문에 장시간 smoke까지는 아직 미완료
  - `PYTHONUTF8=1 python test_auto.py`

## 14. 현재 확인된 강점
- 일본어 VCV/VCV 브리지와 한국어 CV/CVC/CVVC 전반 품질이 초기 버전 대비 크게 개선됨
- MFA 비 ASCII 경로 문제, SOFA 환경 분리, 모델 번들 설치/교체 구조가 정리됨
- ML 기반 수치 보정 계층이 추가되어 규칙 기반 단독보다 더 높은 확장성을 확보함
- `pre` 중심 후처리 정렬 도입으로 사용자가 체감하는 박자 정확도 개선 기반을 마련함
- 일본어 2글자 1모라 조합(`じぇ`, `すぃ` 등)에서 파일명 음절 파싱과 선택기 되감기 문제를 줄이는 안전장치가 추가됨
- 일본어 `VCV` 음절 인덱스 보정은 이제 `기대 인덱스 유지 또는 제한적인 +1 보정`만 허용해, 반복 음절/비음 브리지에서 뒤로 되감기며 전체 타이밍이 무너지는 문제를 줄이도록 수정됨
- 한국어는 `l b`, `ng b` 같은 받침 bridge를 더 이상 `VCV`로 오분류하지 않도록 수정했고, `cv_head + cv + vc` 조합은 `VV`가 없어도 `CVVC`로 판정하도록 형식 감지를 보강함
- 한국어 `CVVC`는 alias 기반 음절열을 더 강하게 우선하도록 조정했고, `VC`는 인접 anchor 평균보다 `이전 모음 끝 + 다음 onset`을 직접 기준으로 계산하는 전용 경로를 추가해 baseline 매핑/연결 안정화를 시도함
- 한국어 `CV/CV_HEAD`는 syllable 첫 phone이 아니라 `실제 onset cluster(마지막 자음 또는 자음+glide)`를 기준으로 onset을 잡도록 수정해, 이전 종성/공백을 onset으로 잘못 포함하는 문제를 줄이도록 조정함
- 한국어 `CV` 매핑은 단모음/이중모음 혼동(`do -> dyo` 등)을 강하게 벌점 주도록 수정했고, `CVVC`에서는 exact vowel 재탐색 범위를 넓혀 모음이 맞는 음절을 더 우선적으로 선택하도록 조정함

## 15. 현재 남은 핵심 이슈
- 한국어 `cvc` ML 모델은 `delta_cutoff` 성능이 아직 불안정함
- 한국어 ML은 전반적으로 일본어보다 일반화 품질이 낮아, 현재는 기본 활성화 대상으로 보기 어려움
- 일본어 `-CV` 컷오프 과단축, `VC/VV/VCV` overlap 개선은 코드 반영 후 실제 보이스뱅크 재검증이 더 필요함
- 오래된 `개발현황_정리.md`는 일부 깨진 텍스트가 남아 있어, 이후 요약/현황 파악은 이 문서를 우선 참조하는 편이 좋음
- 형식별/화자별 미세 차이를 흡수하려면 추가 데이터셋과 재학습이 계속 필요함
- 한국어 `CVVC`는 형식 오분기 문제를 줄였지만, `CV/CV_HEAD` 타이밍 자체는 아직 재생성 결과 기준 추가 검증이 필요함
- 한국어 `CVC`의 `CV` 공백 배치와 한국어 `CVVC`의 `CV/CV_HEAD` 세부 타이밍은 아직 추가 조정이 필요함

## 16. 다음 우선순위 제안
1. 실제 보이스뱅크 재생성으로 `pre` 중심 후처리 결과를 청감/스펙트로그램 기준으로 재검증
2. 한국어 `cvc` cutoff 회귀 원인 분리 및 전용 보강
3. 검증된 ML 번들만 배포용 세트로 고정
4. `개발현황_V2.md`를 기준 문서로 유지하고 큰 변경 시 함께 갱신

## 업데이트
- 한국어 CVVC의 `CV/CV_HEAD` 매핑을 filename occurrence 기반으로 수정했다. 반복 토큰이 있는 파일에서 `도->됴`, `고->교`, `오->요`처럼 line 순서 때문에 발생하던 오매핑을 줄이기 위해, 이제 CVVC는 파일명 토큰 순서로 같은 CV의 등장 횟수를 직접 매핑한다.
- 한국어 CVVC의 `VV`도 filename pair occurrence 기반으로 매핑하도록 수정했다. `i eo`, `eo yeo`, `o yo` 같은 순수 모음 연결은 이제 마지막 모음 하나만 보지 않고 앞뒤 모음 쌍의 등장 순서로 목적지 음절을 직접 찾는다. 동시에 VV 기본 timing을 이전 모음 tail 기준으로 재계산해 offset/cutoff를 더 자연스럽게 잡도록 조정했다.
- 한국어 CVVC의 phones-nuclei 기반 음절열 생성 로직을 강화했다. 기존에는 nuclei를 target 개수에 맞춰 균등 분배해 저볼륨 구간에서 `o/yo`, `eo/yeo` 같은 단모음/반모음 혼동이 남았는데, 이제는 nuclei 주변의 glide와 vowel을 보고 target 음절열에 monotonic alignment를 수행하도록 바꿨다.
- 한국어 CVVC에서 저볼륨 때문에 nuclei 수가 target 음절 수보다 적을 때 전체를 `mapping_failed`로 버리지 않도록 수정했다. 이제는 phone interval을 target 개수에 맞춰 contiguous chunk로 강제 분할해 기본 음절열을 만든 뒤, `CV`/`VV` occurrence map도 `syllables_info`가 아니라 파일명 target 순서를 우선 기준으로 생성한다.
- 한국어 CVVC의 `VC` direct timing을 다시 조정했다. 이전에는 `pre/ovl` 간격이 너무 좁고 coda 종류별 차이가 약해서 연결이 어색했는데, 이제 `stop/nasal/liquid/other`별로 `pre`, `pre-ovl gap`, `cons-pre`, `cutoff` 상한을 분리해 더 compact하고 일관된 브리지 timing을 계산한다.
- 한국어 CVVC의 `VC` cutoff가 후처리 중 다시 길어져 다음 모음 영역까지 포함하는 문제를 줄이기 위해, final post-timing 단계에서 `next onset`과 `다음 자음 끝` 기준의 하드캡을 추가했다. 동시에 `VC`는 base shape blend가 cutoff를 늘리지 못하도록 제한했다.
- 한국어 `VV`만 있는 파일(`a eui`, `u eui`, `ng eui` 등)은 더 이상 `VC_ONLY`로 묶지 않고 `VV_ONLY`로 따로 감지한다. 생성 경로는 동일해도 형식 판단과 로그 표시는 now 정확하게 분리된다.
- 한국어 `CVVC`의 glide 계열 `CV_HEAD(-CV)`는 `초기 멜 가드`가 offset을 과도하게 앞당겨 이후 후단 guard가 다시 되돌리는 패턴이 확인됐다. 현재는 `CV_HEAD`에서 offset soft guard를 생략하고 cutoff soft guard만 유지하도록 조정했다.
## 2026-03-02 추가 업데이트
- 한국어 CVVC 로그 기준 오분기 수정: `a pcl` 같은 closure alias를 VC로 분류하도록 보정해 `pp/tt` 계열 파일이 VCV로 잘못 감지되는 문제를 줄였다.
- 한국어 CVVC의 `CV/CV_HEAD`는 멜 offset soft guard가 공백 쪽으로 과하게 당기는 경향이 있어, CVVC 형식에서는 해당 offset guard를 비활성화하고 cutoff soft guard만 유지하도록 조정했다.

## 2026-03-02 한국어 ML 강화 준비
- OTO ML feature schema를 `v3`로 올리고 `alias_group` feature를 추가했다. 한국어는 `cv`, `cv_glide`, `cv_head`, `cv_head_glide`, `vc_stop`, `vc_sonorant`, `vc_other`, `vv` 등으로 더 세분화해 학습/평가에 활용할 수 있게 했다.
- 학습용 dataset row에 `train_keep_default`, `train_skip_reason`, `train_quality_score`를 추가했다. 한국어는 `CV/CV_HEAD`, `VV`, `VC`별로 극단적인 delta를 가진 row를 기본 제외 대상으로 표시한다.
- `train_oto_ml_model.py`가 `--alias-types`, `--alias-groups`, `--require-train-keep` 옵션을 받도록 확장했다. 이제 한국어는 alias 그룹별/품질 필터 기반으로 더 보수적인 모델 학습이 가능하다.

## 2026-03-02 데이터셋 경로 반영 및 한국어 ML v3 재학습
- `ml/configs/training_data_roots.yaml`에 추가된 새 데이터셋 루트를 반영해 수집을 다시 실행했다. 다만 raw 루트 대부분은 아직 `auto oto + TextGrid` 페어가 없어 직접 학습 대상으로는 바로 쓰이지 않았고, 실제 재학습은 `Auto_OTO/dataset`의 staged 데이터셋 기준으로 다시 수행했다.
- staged 데이터셋을 `feature_version=v3` 기준으로 재수집했고, 한국어 학습 CSV를 다시 구축했다.
  - `dataset_korean_cvc.csv`: `2013` rows
  - `dataset_korean_cvvc.csv`: `5604` rows
  - `dataset_korean_general.csv`: `7617` rows
- 한국어 ML은 alias 그룹과 품질 필터를 기준으로 다시 학습했다.
  - `korean/cvc/profile_run_v3`: `vc_stop`, `vc_sonorant`만 학습, `train_rows=53`
  - `korean/cvvc/profile_run_v3`: `vv`, `vc_stop`, `vc_sonorant`만 학습, `train_rows=530`
  - `korean/general/profile_run_v3`: `require_train_keep`를 걸어 재학습했지만 holdout 품질이 충분히 안정적이지 않아 설치 대상에서는 제외했다.
- 새 한국어 `cvvc` 후보 번들은 배포용으로 export 후 런타임 설치 경로에 반영했다.
  - 설치 경로: `models_installed/oto_ml/korean/cvvc/v1`
  - 현재 설치 메타: `feature_version=v3`, `train_rows=530`, `voicebank_count=3`
- `korean/cvc/profile_run_v3`는 holdout 지표 자체는 좋아졌지만 실제 학습 그룹이 `1` 화자뿐이라 과적합 위험이 높다. 그래서 이번 단계에서는 export만 해두고 런타임 `cvc/v1` 교체는 보류했다.
- 현재 런타임 사용 방침:
  - `korean/cvvc/v1`: 새 `v3` 후보 사용
  - `korean/cvc/v1`: 기존 `v2` 유지
  - `korean/general/v1`: 기존 `v2` 유지
- 한국어 CVVC의 `VV` baseline 버그를 수정했다. 기존에는 `vv` alias가 실제 생성 경로에서 `vc` 공용 컨텍스트로 들어가 `pair occurrence`와 `VV direct timing`이 사실상 무시되고 있었는데, 이제 `VV`는 `VC` bridge 컨텍스트에서 분리되어 filename pair occurrence와 전용 timing 경로를 실제로 사용한다. 이 수정은 같은 모음 반복 파일에서 `VV`가 같은 위치로 몰리거나 공백을 포함하던 현상에 직접 대응한다.

## 2026-03-02 PyTorch 브리지 보정 파이프라인 구현
- 브리지 전용 PyTorch OTO 보정 파이프라인 골격을 실제 코드로 구현했다. 신규 core 모듈은 `oto_torch_features.py`, `oto_torch_dataset.py`, `oto_torch_model.py`, `oto_torch_trainer.py`, `oto_torch_export.py`이며, 기존 `oto_ml_pytorch.py` stub도 실제 bundle load/predict 구현으로 교체했다.
- 신규 CLI 스크립트를 추가했다.
  - `ml/scripts/build_oto_torch_dataset.py`
  - `ml/scripts/train_oto_torch_model.py`
  - `ml/scripts/evaluate_oto_torch_model.py`
  - `ml/scripts/export_oto_torch_bundle.py`
- PyTorch 학습은 `small CNN + tabular fusion` 구조로 고정했다.
  - 입력: `80 x 160` log-mel window + 기존 numeric feature + categorical embedding + `voicebank_id` embedding
  - 출력: `delta_offset`, `delta_cons`, `delta_cutoff`, `delta_pre`, `delta_ovl`
- 기존 OTO ML feature schema를 `v4`로 올리고, PyTorch용 신뢰도/음량 feature를 추가했다.
  - `mapping_confidence`
  - `used_alias_occurrence_mapping`
  - `used_exact_vowel_fix`
  - `used_nuclei_fallback`
  - `used_alias_based_syllables`
  - `words_vs_alias_score_margin`
  - `local_peak_db`
  - `local_valley_db`
  - `mel_window_energy_mean`
  - `mel_window_silence_ratio`
- `Auto_OTO/.cache/oto_torch_windows`에 per-wav mel cache를 만들고, 학습 입력은 voicebank 단위 `.npz` shard + `index.json` 구조로 저장하도록 했다.
- 런타임은 아직 기본 OFF 정책을 유지한다. 다만 `core/oto_ml_runtime.py`를 통해 `backend="pytorch"` 번들을 실제로 로드하고 `predict_oto_deltas()`를 호출할 수 있는 상태까지 구현했다.
- 실데이터 스모크 검증 결과:
  - `japanese_bridge_cvvc` dataset build: `1047 rows`, `2 voicebanks`
  - `korean_bridge_cvvc` dataset build(v4 재수집 후): `1293 rows`, `6 voicebanks`
  - `korean_bridge_cvc` dataset build(v4 재수집 후): `71 rows`, `2 voicebanks`
- 실GPU 학습 스모크 결과:
  - `RTX 3060 Ti 8GB`에서 CUDA 인식 정상
  - `japanese_bridge_cvvc` 1 epoch 학습 성공 (`train_rows=183`)
  - `korean_bridge_cvvc` 1 epoch 학습 성공 (`train_rows=765`)
  - 두 태스크 모두 `evaluate -> export -> runtime load/predict`까지 실제로 동작 확인
- 한국어 브리지 태스크는 LightGBM용 `train_keep_default`가 지나치게 보수적이라 데이터가 거의 사라졌다. PyTorch 쪽은 브리지 전용이라는 목적에 맞춰 `train_keep_default` 의존을 끄고, 대신 `mapping_confidence >= 0.3` + alias/coda 필터를 우선 사용하는 정책으로 조정했다.
- 관련 테스트를 추가해 파이프라인 회귀를 막았다.
  - `ml/tests/test_torch_dataset.py`
  - `ml/tests/test_torch_bundle.py`
  - `ml/tests/test_torch_runtime.py`
- 현재 판단:
  - `RTX 3060 Ti 8GB`로 초기 브리지 전용 학습/평가/익스포트는 충분히 가능하다.
  - 대여 GPU는 larger-context window, Transformer/Conformer, 대규모 튜닝으로 넘어가기 전까지는 필수 아님.
- 앱 런타임에는 임시 실험용 토글을 추가했다.
  - 기존 `OTO ML 수치 보정 적용`은 전체 ML on/off 역할을 유지
  - 새 `PyTorch 브리지 보정 사용 (실험용)` 토글을 켜면, 브리지 계열에서 PyTorch 번들을 우선 찾고 없으면 기존 LightGBM으로 fallback한다.
  - 현재 기본값은 OFF이며, config 저장/복원이 연결되어 있다.

## 2026-03-02 PyTorch 런타임 테스트 후기 요약
- 한국어 `CVVC`
  - PyTorch 브리지 보정 적용 시 이전 LightGBM 기반 보정보다 품질이 더 좋았다.
  - `VC` 연결은 아직 다소 어색하고, 동일한 위치에서 음절 매핑을 틀리는 증상은 남아 있다.
  - 그래도 실사용 가능한 수준이며, 발음은 충분히 정확하게 들릴 정도로 개선 효과가 확인됐다.
- 한국어 `CVC`
  - 청감상 품질은 기존 ML 보정과 비슷한 수준이지만, 매핑 경향은 다르게 나타난다.
  - `VC` 연결 정확도는 더 좋지만, 종성이 포함된 `C-VC` 또는 `V-C` alias는 아직 어색하다.
  - 현재 추정 원인:
    - 받침 영역을 너무 짧게 잡는 경향
    - 단모음/이중모음 단독에서 overlap과 고정영역을 너무 짧게 잡는 경향
- 일본어 `CVVC`
  - `VC` 설정이 아직 약간 끊기는 느낌이 남아 있다.
  - 전체 체감 품질은 기존 ML과 비슷하지만, `CV` 정확도는 PyTorch 쪽이 더 좋았다.
- 일본어 `VCV`
  - 일반적인 녹음에서는 선행발음이 약간 늦거나 너무 앞서는 경우가 있지만, 실사용에 지장이 있는 수준은 아니었다.
  - 전반적으로 들을 만한 품질로 평가됐다.
- 특이 발성/극단 조건 대응
  - PyTorch 모델은 특이한 발성, 특이한 발음, 매우 작은 볼륨 같은 특수 조건에서 더 효과적인 경향이 확인됐다.
  - 특히 이전 ML로는 타이밍이 거의 맞지 않던 고음·특이 발성 `VCV` 보이스뱅크 하나에서 결과가 꽤 개선됐다.
  - 다만 그 경우에도 완전한 해결은 아니며, 일부 틀린 매핑은 남아 있다.
- 현재 해석
  - PyTorch 브리지 보정은 `VC/VV/VCV`의 연결 품질과 특수 화자 적응 면에서 확실한 가능성이 있다.
  - 다만 한국어는 여전히 baseline 음절 매핑 문제가 남아 있어, 브리지 보정만으로는 전체 문제를 해결할 수 없다.

## 2026-03-02 한국어 우선 baseline 보정 추가
- 한국어 `CVVC`의 `CV/CV_HEAD`에서 filename occurrence로 이미 강제 매핑된 인덱스를, 뒤의 `exact vowel fix`가 다시 덮어쓰는 경로를 차단했다.
- 의도:
  - `도 -> 됴`, `고 -> 교`, `러 -> 려`처럼 occurrence 기준으로 맞게 잡힌 `CV/CV_HEAD`가 다시 밀리는 회귀를 줄이기 위함.
- 구현:
  - `core/oto_generator.py`
  - `_should_allow_kr_exact_vowel_fix(...)` 추가
  - `CVVC + forced occurrence`일 때는 `exact vowel fix`를 비활성화
- 회귀 테스트 추가:
  - `ml/tests/test_feature_extraction.py`
  - `CVVC + forced occurrence -> exact vowel fix 차단`

## 2026-03-02 PyTorch 브리지 분리 학습 준비
- PyTorch bridge task를 `VV / VC / VCV / -CV` 단위로 더 세분화했다.
- 추가 task:
  - 일본어
    - `japanese_bridge_vcv_vv`
    - `japanese_bridge_vcv_vc`
    - `japanese_bridge_vcv_vcv`
    - `japanese_bridge_vcv_cv_head`
    - `japanese_bridge_cvvc_vv`
    - `japanese_bridge_cvvc_vc`
    - `japanese_bridge_cvvc_cv_head`
  - 한국어
    - `korean_bridge_cvvc_vv`
    - `korean_bridge_cvvc_vc`
    - `korean_bridge_cvc_vc`
- 한국어는 필터도 강화했다.
  - `korean_bridge_cvvc_vv`
    - `mapping_confidence >= 0.45`
    - `used_nuclei_fallback == 0`
    - `used_alias_occurrence_mapping == 1`
  - `korean_bridge_cvvc_vc`, `korean_bridge_cvc_vc`
    - `mapping_confidence >= 0.45`
    - `used_nuclei_fallback == 0`
    - `coda_type in {stop, nasal, liquid}`
- 의도:
  - 한국어는 `VV`와 `VC`의 오류 성격이 달라 하나의 브리지 모델로 학습시키면 서로 간섭이 커진다.
  - 특히 `VV`는 occurrence 기반으로 맞게 매핑된 고신뢰 샘플만 따로 학습해야 한다.

## 2026-03-02 PyTorch 브리지 배치 학습 스크립트 추가
- `ml/scripts/run_oto_torch_batch.py` 추가
- 역할:
  - 지정한 task 목록을 순차적으로
    1. torch dataset build
    2. train
    까지 자동 실행
- 기본 실행 시 한국어 우선 3개 task를 순서대로 처리한다.
  - `korean_bridge_cvvc_vv`
  - `korean_bridge_cvvc_vc`
  - `korean_bridge_cvc_vc`
- task별 기본 하이퍼파라미터도 내장해 두었다.
  - 한국어 `CVVC VV/VC`: `20 epoch`, `batch 32`
  - 한국어 `CVC VC`: `28 epoch`, `batch 16`

## 2026-03-02 한국어 PyTorch VC 분리 강화
- 한국어 `CVVC`, `CVC`의 `VC`를 다시 `sonorant / stop`으로 분리한 PyTorch task를 추가했다.
- 추가 task:
  - `korean_bridge_cvvc_vc_sonorant`
  - `korean_bridge_cvvc_vc_stop`
  - `korean_bridge_cvc_vc_sonorant`
  - `korean_bridge_cvc_vc_stop`
- 필터 임계값도 분리했다.
  - `sonorant`
    - `mapping_confidence >= 0.50`
    - `used_nuclei_fallback == 0`
    - `coda_type in {nasal, liquid}`
  - `stop`
    - `mapping_confidence >= 0.60`
    - `used_nuclei_fallback == 0`
    - `coda_type == stop`
- 이유:
  - 기존 `korean_bridge_cvvc_vc` 평가에서 `vc_sonorant`는 유망했지만 `vc_stop`은 오히려 악화되는 경향이 확인됐다.
  - stop 받침과 sonorant 받침은 timing 성격이 달라 하나의 모델로 학습시키면 평균화로 성능이 무너진다.
- 배치 학습 기본 순서도 한국어 우선 split task 기준으로 변경했다.
  - `korean_bridge_cvvc_vv`
  - `korean_bridge_cvvc_vc_sonorant`
  - `korean_bridge_cvvc_vc_stop`
  - `korean_bridge_cvc_vc_sonorant`
  - `korean_bridge_cvc_vc_stop`

## 2026-03-02 한국어 PyTorch VC stop 재학습 준비
- `vc_stop` 모델은 이전 평가에서 `cons/pre/ovl`가 심하게 폭주했기 때문에, stop 전용 필터와 loss 프로파일을 추가했다.
- 데이터 필터 강화:
  - `alias_group == vc_stop` 강제
  - `mapping_confidence >= 0.60`
  - `used_nuclei_fallback == 0`
  - 극단 target delta row 제외
    - `delta_offset <= 1800` (`CVC`는 `2400`)
    - `delta_cons <= 220`
    - `delta_cutoff <= 1800` (`CVC`는 `2400`)
    - `delta_pre <= 160`
    - `delta_ovl <= 90`
- trainer loss도 task별로 분리했다.
  - `vc_stop`
    - `offset/cutoff` 가중치 강화
    - `pre/ovl` 가중치 완화
    - `constraint_loss` 가중치 강화
    - `rhythm_loss`는 약하게
  - `vc_sonorant`
    - `pre/ovl`과 bridge body를 더 중시하는 완화형 프로파일 사용
- 목적:
  - stop 받침 모델이 극단 outlier를 학습해서 `pre/ovl`가 수천 ms 단위로 붕괴하던 문제를 줄이기 위함.

## 2026-03-02 한국어 CVC PyTorch 재학습 결과
- `korean_bridge_cvc_vc_sonorant/v1`
  - `cons`, `pre`, `ovl`은 의미 있게 개선
  - `offset`, `cutoff`는 거의 변하지 않음
  - 해석:
    - 위치 축을 바로잡는 모델은 아니지만, sonorant bridge의 내부 timing 보정기 역할은 가능
- `korean_bridge_cvc_vc_stop/v1`
  - `offset`, `cutoff`, `pre`, `ovl`은 소폭 개선
  - `cons`만 약간 악화
  - 이전 대비 stop 모델의 폭주는 해소됨
- 공통 주의점
  - 데이터셋 row 수가 매우 작다
    - `vc_sonorant`: 6 rows
    - `vc_stop`: 13 rows
  - 따라서 수치가 좋아져도 일반화 성능은 아직 강하게 신뢰하기 어렵다

## 2026-03-02 한국어 생성기 리팩토링 2차
- 한국어 생성기에서 `CV/CV_HEAD` 매핑, filename occurrence, `VV` pair occurrence, alias 기반 음절열 선택 관련 헬퍼를 새 모듈 [core/kr_oto_mapping.py](C:\Users\oyh57\SODAsoo1\Devs\UTAU_Auto_OTO_v3\Auto_OTO\core\kr_oto_mapping.py)로 분리했다.
- [core/oto_generator.py](C:\Users\oyh57\SODAsoo1\Devs\UTAU_Auto_OTO_v3\Auto_OTO\core\oto_generator.py) 내부의 중복 정의는 제거하고, 새 모듈 import 경로만 사용하도록 정리했다.
- 목적:
  - 한국어 `CV/CV_HEAD`, `VV`, `VC` 매핑 문제를 각각 독립적으로 수정할 수 있게 구조를 정리
  - 회귀 범위를 줄이고, 이후 `VC/VV` timing 리팩토링과 baseline 개선 작업을 더 안전하게 진행하기 위함

## 2026-03-02 한국어 생성기 리팩토링 3차
- 한국어 브리지 timing 관련 헬퍼를 새 모듈 [core/kr_oto_bridge.py](C:\Users\oyh57\SODAsoo1\Devs\UTAU_Auto_OTO_v3\Auto_OTO\core\kr_oto_bridge.py)로 분리했다.
- 분리한 범위:
  - `VC/VV` 인접 CV anchor 기반 timing
  - `CVVC VC/VV` direct timing
  - pre 중심 재정렬
  - 한국어 자음 성질 기반 timing shaping
- [core/oto_generator.py](C:\Users\oyh57\SODAsoo1\Devs\UTAU_Auto_OTO_v3\Auto_OTO\core\oto_generator.py)에서는 위 브리지 전용 함수들의 중복 정의를 제거하고 새 모듈 import만 사용하도록 정리했다.
- 목적:
  - 한국어 `VC/VV` timing 수정과 baseline 품질 개선을 더 작은 범위에서 안전하게 반복할 수 있게 만들기 위함

## 2026-03-02 한국어/일본어 생성기 리팩토링 4차
- 한국어 `CV/CV_HEAD` timing 관련 헬퍼를 새 모듈 [core/kr_oto_cv.py](C:\Users\oyh57\SODAsoo1\Devs\UTAU_Auto_OTO_v3\Auto_OTO\core\kr_oto_cv.py)로 분리했다.
- 분리한 범위:
  - `CV` timing 계산
  - `CV` anchor 추정
  - `CV_HEAD` 기본 timing 산출
  - onset slice 선택
  - syllable에서 `CV` bounds 추출
- [core/oto_generator.py](C:\Users\oyh57\SODAsoo1\Devs\UTAU_Auto_OTO_v3\Auto_OTO\core\oto_generator.py)에서는 위 `CV/CV_HEAD` 전용 함수의 중복 정의를 제거하고 새 모듈 import만 사용하도록 정리했다.
- 일본어 쪽은 최종 cutoff 안전화/저장 경로를 새 모듈 [core/ja_oto_finalize.py](C:\Users\oyh57\SODAsoo1\Devs\UTAU_Auto_OTO_v3\Auto_OTO\core\ja_oto_finalize.py)로 분리했다.
- 분리한 범위:
  - `sanitize_ja_oto_for_wav_duration`
  - `_sanitize_ja_internal_params_for_wav_duration`
  - `_convert_ja_internal_cutoff_to_oto_field`
- 목적:
  - 한국어는 `CV/CV_HEAD`와 `VC/VV`를 완전히 다른 수정 단위로 다룰 수 있게 만들기 위함
  - 일본어는 cutoff 좌표계/최종 저장 관련 회귀를 생성 본체와 분리해 더 안전하게 관리하기 위함

## 2026-03-02 ML feature 리팩토링 1차
- [core/oto_ml_features.py](C:\Users\oyh57\SODAsoo1\Devs\UTAU_Auto_OTO_v3\Auto_OTO\core\oto_ml_features.py)에서 신뢰도/매핑 품질 계산부를 새 모듈 [core/oto_ml_mapping_quality.py](C:\Users\oyh57\SODAsoo1\Devs\UTAU_Auto_OTO_v3\Auto_OTO\core\oto_ml_mapping_quality.py)로 분리했다.
- 분리한 범위:
  - `mapping_confidence`
  - `used_alias_occurrence_mapping`
  - `used_exact_vowel_fix`
  - `used_nuclei_fallback`
  - `used_alias_based_syllables`
  - `words_vs_alias_score_margin`
  - `local_peak_db`, `local_valley_db`, `mel_window_energy_mean`, `mel_window_silence_ratio`
- 목적:
  - 한국어/일본어 학습 필터 기준을 조정할 때 `oto_ml_features.py` 전체를 건드리지 않게 만들기 위함
  - LightGBM/PyTorch 공통 신뢰도 feature를 더 작은 단위로 유지보수하기 위함

## 2026-03-02 일본어 생성기 리팩토링 5차
- 일본어 매핑/브리지 헬퍼를 생성기 본체에서 분리해 재수출하는 구조로 정리했다.
- 새 모듈 [core/ja_oto_mapping.py](C:\Users\oyh57\SODAsoo1\Devs\UTAU_Auto_OTO_v3\Auto_OTO\core\ja_oto_mapping.py)에 분리한 범위:
  - `classify_ja_alias`
  - `detect_ja_alias_format`
  - `get_vc_consonant`
  - `VCV/CV` 음절 선택 및 filename/nuclei 기반 매핑 보조 함수
- 새 모듈 [core/ja_oto_bridge.py](C:\Users\oyh57\SODAsoo1\Devs\UTAU_Auto_OTO_v3\Auto_OTO\core\ja_oto_bridge.py)에 분리한 범위:
  - `VC/VV/VCV` overlap shaping
  - pre 중심 재정렬
  - `CV/CV_HEAD` onset class, offset/pre 계산
  - target vowel / CV bounds 추출
- [core/ja_oto_generator.py](C:\Users\oyh57\SODAsoo1\Devs\UTAU_Auto_OTO_v3\Auto_OTO\core\ja_oto_generator.py)는 새 모듈의 함수를 다시 export하도록 연결해 외부 API(`core.ja_oto_generator` import)는 그대로 유지했다.
- [core/oto_ml_features.py](C:\Users\oyh57\SODAsoo1\Devs\UTAU_Auto_OTO_v3\Auto_OTO\core\oto_ml_features.py)는 일본어 alias 분류/포맷 감지를 더 이상 생성기 본체에 의존하지 않고 `ja_oto_mapping`을 직접 참조하도록 정리했다.
- [core/ja_oto_finalize.py](C:\Users\oyh57\SODAsoo1\Devs\UTAU_Auto_OTO_v3\Auto_OTO\core\ja_oto_finalize.py)도 일본어 alias 분류를 `ja_oto_mapping`에서 직접 읽도록 정리했다.
- 목적:
  - 일본어 생성기에서 매핑/브리지/최종 cutoff 경로를 서로 독립적으로 수정할 수 있게 만들기 위함
  - ML feature 계산이 생성기 대형 모듈에 덜 묶이도록 해 리팩토링 범위를 줄이기 위함

## 2026-03-02 일본어 생성기 리팩토링 6차
- 일본어 후처리 중첩 함수를 새 모듈 [core/ja_oto_postprocess.py](C:\Users\oyh57\SODAsoo1\Devs\UTAU_Auto_OTO_v3\Auto_OTO\core\ja_oto_postprocess.py)로 분리했다.
- 분리한 범위:
  - `post_adjust_params`
  - `CV_HEAD` 최소 모음 보존용 cutoff 하한 계산
  - `CV/CV_HEAD` cutoff-to-next-onset 가드
  - `CV_HEAD` offset-to-onset 가드
  - `CV_HEAD` 최소 모음 포함 보정
- [core/ja_oto_generator.py](C:\Users\oyh57\SODAsoo1\Devs\UTAU_Auto_OTO_v3\Auto_OTO\core\ja_oto_generator.py) 안에는 위 함수의 실제 로직 대신, 현재 파일 컨텍스트(`syllables_info`, `phone_spans_ms`, style/autotune 상태)를 넘기는 얇은 래퍼만 남겼다.
- 함께 일본어 ML delta 정책 회귀를 복구해:
  - `VCV`의 `n+유성음` 브리지는 위치 축을 더 강하게 감쇠
  - `CVVC`의 치찰음/파열음 `cv/cv_head`는 offset delta를 다시 0으로 고정
- 목적:
  - 일본어 `cutoff/offset` 후처리 회귀를 생성 본체와 분리해 더 작은 범위에서 수정할 수 있게 만들기 위함
  - 일본어 ML 보정 정책 테스트와 생성기 리팩토링이 서로 덜 충돌하도록 만들기 위함
