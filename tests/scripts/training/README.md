# Training Test Scripts

Experimental training helpers moved out of `scripts/train/`.

- `alignment/`: label conversion/preprocess/profile helpers
- `sequence/`: sequence residual dataset/preprocess/baseline helpers
- `data/`: data classification/profile generation helpers
- `coupled/`: ensemble training helper for experiments
