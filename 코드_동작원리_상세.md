# Auto_OTO 코드 동작원리 상세 문서

> 주의: 2026-03-08 기준으로 현재 OTO 자동 생성 보정 런타임은 `LightGBM`만 사용한다. 아래의 PyTorch 관련 설명은 과거 구조 설명 보존용이다.


## 1. 문서 목적
- 이 문서는 현재 `Auto_OTO` 코드베이스가 어떤 방법론으로 OTO를 생성/보정하는지, 실제 실행 순서 기준으로 설명한다.
- 특히 “한 보이스뱅크를 넣었을 때 내부에서 어떤 단계가 어떤 함수로 실행되는지”를 추적 가능하게 정리한다.

## 2. 핵심 방법론 요약
`Auto_OTO`는 아래 3계층 구조를 사용한다.

1. 정렬 기반 규칙 엔진
- TextGrid(또는 LAB/words/phones)를 기준으로 alias를 음절/음소 경계에 매핑한다.
- 언어/형식(CVC, CVVC, VCV 등)/alias_type(CV, VC, VV, VCV, CV_HEAD...)별 규칙으로 기본 파라미터를 계산한다.

2. 후처리 안전 가드
- 계산된 파라미터가 다음 음절을 침범하거나 공백만 잡지 않도록 cutoff/offset/pre/overlap 관계를 재조정한다.
- 박자감(특히 pre-ovl 간격)과 렌더 안정성을 해치지 않도록 clamp/recenter를 적용한다.

3. 선택형 ML 델타 보정
- 기본 계산 결과를 대체하지 않고 “delta(보정량)”만 추가 적용한다.
- 모델이 없거나 비활성화 시 규칙 기반 결과만으로 동작한다.

## 3. 메인 진입점과 탭/액션 구조
- GUI 진입점: `main.py`
- UI 액션은 `ui/*_mixin.py`로 분리되어 있고, 실제 생성/정렬은 `core/` 모듈을 호출한다.
- 대형 작업(정렬/자동 OTO/ML 처리)은 UI에서 상태 메시지만 표시하고, 계산 로직은 대부분 `core/`에 있다.

## 4. 한 파일(한 wav 그룹) 기준 OTO 생성 순서

## 4-1. 입력 준비
1. 템플릿 OTO line 로딩
- `core/textio_utils.py`의 로더를 통해 기준 line들을 읽는다.

2. TextGrid/phone tier 확보
- 정렬 결과에서 phones/words tier를 읽어 시간축 기준 데이터를 만든다.
- 비어 있거나 tier가 부족하면 파일 단위 fallback(원본 line 유지 또는 skip) 처리한다.

## 4-2. 형식/alias 분류와 매핑
1. 형식 감지
- 한국어: `core/kr_oto_rules.py`의 `detect_alias_format`
- 일본어: `core/ja_oto_mapping.py`의 `detect_ja_alias_format`

2. alias_type 분류
- 한국어: `classify_alias`
- 일본어: `classify_ja_alias`

3. 음절 인덱스 매핑
- 파일명/occurrence/phones 핵(nucleus) 정보를 조합해 alias가 가리킬 음절 인덱스를 결정한다.
- 한국어 매핑 로직: `core/kr_oto_mapping.py`
- 일본어 매핑 로직: `core/ja_oto_mapping.py`

## 4-3. 1차 파라미터 계산(규칙 기반 baseline)
1. 한국어
- CV/CV_HEAD 계산: `core/kr_oto_cv.py`
- VC 계산: `core/kr_oto_vc.py`
- VV 계산: `core/kr_oto_vv.py`
- 브리지 성질 보정: `core/kr_oto_bridge.py`

2. 일본어
- 매핑 보조/형식 분기: `core/ja_oto_mapping.py`
- VC/VV/VCV 중심 계산: `core/ja_oto_bridge.py`
- 생성 본체 루프: `core/ja_oto_generator.py`

## 4-4. 2차 후처리(안전화/재정렬)
1. 한국어 후처리 컨텍스트
- `core/kr_oto_postprocess.py`의 `KrPostprocessContext`
- 적용 항목:
  - soft mel 기반 offset/cutoff 1차 가드
  - base shape blend
  - phone activity stabilize
  - pre 중심 recenter
  - VC cutoff next-segment 가드
  - CV/CV_HEAD cutoff next-onset 가드

2. 일본어 후처리 컨텍스트
- `core/ja_oto_postprocess.py`의 `JaPostprocessContext`
- 적용 항목:
  - post_adjust(시간축 재안정화)
  - CV_HEAD 최소 모음 포함 보장
  - CV/CV_HEAD cutoff-to-next-onset 가드
  - CV_HEAD offset-to-onset 가드

## 4-5. validate/finalize와 저장
1. 공통 validate
- `offset >= 0`, `consonant >= pre`, `cutoff_abs > consonant` 같은 기본 제약을 강제한다.

2. 일본어 finalize
- `core/ja_oto_finalize.py`에서 내부 cutoff 좌표계를 최종 OTO 필드 좌표계로 정리하고 wav 길이 초과를 방지한다.

3. OTO line 기록
- alias suffix/OpenUtau alias 옵션 등을 반영해 최종 line을 쓴다.

## 5. ML/딥러닝 보정 계층 동작

## 5-1. 공통 컨셉
- ML은 baseline을 “교체”하지 않는다.
- baseline 파라미터에 target별 delta를 더하는 방식이다.

보정 대상 5개:
1. `offset`
2. `consonant`
3. `cutoff`
4. `preutter`
5. `overlap`

## 5-2. 런타임 라우팅
- `core/oto_ml_runtime.py`가 모델 번들 선택/로딩/예측을 통합한다.
- backend는 `lightgbm` 또는 `pytorch` 메타로 분기한다.
- 최종 적용은 `core/oto_ml_refiner.py`에서 row별로 수행한다.

## 5-3. LightGBM 파이프라인
- feature 생성: `core/oto_ml_features.py`
- dataset 구성: `core/oto_ml_dataset.py`
- 학습/평가: `core/oto_ml_lightgbm.py`
- export/install: `core/oto_ml_export.py`, `core/oto_ml_bundle_install.py`

## 6. 데이터 추가 후 재학습 표준 흐름
상세 절차는 `ml/학습_재빌드_절차.md`를 따른다.

요약 순서:
1. `stage_training_sources.py`
2. `prepare_staged_auto_pairs.py`
3. `collect_oto_ml_training_data.py`
4. 필요 시 `train_oto_ml_model.py`
5. 평가/번들 export/install

## 7. 최근 리팩토링 방향
1. 대형 생성기에서 기능 분리
- 한국어: `kr_oto_*` 모듈
- 일본어: `ja_oto_*` 모듈

2. 후처리 컨텍스트 객체화
- 한국어/일본어 모두 “중첩 로컬 함수”를 줄이고 모듈 메서드 중심으로 전환

3. 호환 레이어 유지
- 테스트/외부 스크립트 경로가 깨지지 않도록 compatibility export를 일부 유지

## 8. 디버깅 우선순위 가이드
품질 문제가 생겼을 때는 아래 순서로 확인한다.

1. 매핑 정확도
- alias가 올바른 음절 인덱스를 가리키는지 확인

2. baseline 계산
- CV/VC/VV/VCV 분기에서 타입 오판정이 없는지 확인

3. 후처리 가드 과보정 여부
- cutoff/offset 가드가 과도하게 압축/확장하지 않는지 확인

4. ML 보정 영향
- 토글 OFF/ON A/B 비교로 baseline 문제인지 보정 문제인지 분리

## 9. 운영 원칙
1. 기본 규칙 엔진만으로도 “실사용 가능한 품질”을 유지한다.
2. ML/딥러닝은 가산 보정 계층으로만 운용한다.
3. 불안정 회귀가 발생하면 후처리/모델보다 먼저 매핑 품질부터 점검한다.


