# Auto_OTO 개발현황 V3_최신

## 1. 문서 목적
- 이 문서는 `Auto_OTO`의 **현재 동작 상태**와 **다음 작업 우선순위**만 빠르게 확인하기 위한 운영 문서다.
- 상세 변경 이력은 `개발현황_V2.md`, `개발현황_정리.md`를 참고한다.

## 2. 현재 상태 요약 (2026-03 기준)
- 앱 기본 파이프라인은 정상 동작:
  1. Lab 생성
  2. Dictionary 생성
  3. MFA/SOFA 정렬(TextGrid)
  4. OTO 1차 생성(규칙 기반)
  5. 후처리(guard/anchor lock/검증/선택적 ML)
- 언어/형식 지원:
  - 한국어: `CVC`, `CVVC` (내부 alias: `cv`, `cv_head`, `vc`, `vv`, `vcv`, `mono`, `br`)
  - 일본어: `VCV`, `CVVC` (동일 alias 계열 지원)
- ML 계층:
  - LightGBM 런타임 지원
  - PyTorch OTO 보정 파이프라인은 제거됨

## 3. 품질 현황 (체감 기준 요약)
### 일본어
- `CVVC`: 전반적으로 양호, 다만 일부 `VC` 연결의 자연스러움 개선 여지.
- `VCV`: 이전 대비 안정화되었지만, 일부 은행에서 음절 밀림/오매핑이 간헐적으로 발생.
- ML 보정은 `VV`, `VC`에서 개선 체감이 큼.

### 한국어
- `CVC/CVVC`: 대규모 음절 오매핑은 감소.
- 남은 핵심 문제는 `VV/VC/받침 연결`의 자연스러움과 타이밍 일관성.
- LightGBM은 형식별 편차가 있어, 데이터 품질/가중치 전략이 성능을 크게 좌우.

## 4. 핵심 아키텍처 (현재)
- 규칙 기반 생성:
  - 한국어: `core/oto_generator.py` + KR bridge/cv 모듈
  - 일본어: `core/ja_oto_generator.py` + JA bridge/mapping 모듈
- 타이밍 안정화:
  - `core/timing_anchor_profiles.py`
  - `core/timing_anchor_runtime.py`
  - `pre(abs)` 기준 재정렬 + 상대 gap 재구성
- ML 런타임:
  - `core/oto_ml_refiner.py`
  - `core/oto_ml_runtime.py`
  - same-language 우선 라우팅(교차 언어 차용 기본 금지)

## 5. 최근 반영된 중요 변경
- KR VCV 앵커 프로파일 외부 로드 지원:
  - `ml/configs/kr_vcv_anchor_profile.yaml`
  - env: `UTOA_KR_ANCHOR_PROFILE_PATH`
- KR 매핑 점프 제한 강화:
  - 기본 `±1`, 고신뢰 `±2`
  - 신뢰도 기반 이동폭 축소
- LightGBM 학습 데이터 확장:
  - 컬럼: `label_source`, `sample_weight`, `mapping_reason_code`, `jump_blocked_flag`
  - 학습 시 sample weight 반영
  - 옵션: `--use-pseudo-labels`, `--pseudo-weight-high`, `--pseudo-weight-mid`
- 분석 스크립트 추가:
  - `scripts/analyze_oto_diff.py`
  - `scripts/analyze_kr_mapping_jumps.py`
  - `scripts/build_kr_vcv_profile_from_oto.py`

## 6. 데이터/학습 운영 정책 (현재)
- 코드와 실험 산출물 분리:
  - 코드/런타임: `Auto_OTO/`
  - 대용량 실험: `../ml_workspace/`
- 스테이징 우선:
  - `training_data_roots.yaml` -> `dataset/` 스테이징 -> auto/TextGrid 준비 -> CSV 재수집
- LightGBM:
  - 형식 전용 모델 + 언어 공용 fallback 조합 유지
  - 저신뢰 pseudo는 가중치 0 또는 제외

## 7. 현재 리스크/병목
- 리스크 1: 특정 보이스뱅크 정렬 품질 저하
  - 결과: 동일 규칙이라도 매핑 오차가 급증.
- 리스크 2: 형식/화자 편차가 큰 pseudo 데이터 유입
  - 결과: 재학습 후 품질 역전(나빠짐) 가능.
- 리스크 3: 일본어 VCV 일부 파일의 파일명/TextGrid 매칭 실패
  - 결과: `mapping_failed`, `textgrid_missing` 증가.

## 8. 다음 작업 우선순위
### P0 (즉시)
- KR `VC/VV/받침` 연결 자연스러움 개선(규칙식 우선).
- JP `CVVC VC` 연결 로직 보강(CV anchor 기반).
- 배치 청음 케이스로 형식별 회귀 테스트 자동화 유지.

### P1 (단기)
- LightGBM 재학습 사이클 고정:
  - 데이터셋 재수집
  - 형식별 평가
  - 실사용 번들 설치
- `mapping_failed` reason 통계 기반 필터 튜닝.

### P2 (중기)
- KR/JP 공통 브리지 규칙 코어 정리(공통 + 언어별 보정 분리).

## 9. 다음 마일스톤 완료 기준
- KR `CVC/CVVC`:
  - validator `error=0` 유지
  - `VC cutoff leak` 및 박자 어색함 체감 감소
- JP `VCV/CVVC`:
  - 대표 오매핑 케이스(1칸 이상 밀림) 재발률 유의미 감소
- ML:
  - 재학습 후 baseline 대비 `pre/ovl/cutoff` MAE 퇴화 없음

## 10. 빠른 실행 체크리스트
1. `python ml\scripts\stage_training_sources.py`
2. `python ml\scripts\prepare_staged_auto_pairs.py`
3. `python ml\scripts\collect_oto_ml_training_data.py`
4. LightGBM 재학습 (`train_oto_ml_model.py`)
5. 번들 export/install 후 앱에서 OTO 재생성/검증
6. `scripts/analyze_oto_diff.py`로 이상치 확인

## 11. 참고 문서
- `개발현황_V2.md`: 상세 이력/중간 의사결정
- `개발현황_정리.md`: 누적 상세 메모
- `코드_동작원리_상세.md`: 엔진 동작 원리
- `ml/학습_재빌드_절차.md`: 데이터 추가/재학습 절차

